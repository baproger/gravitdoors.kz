# Gravit ERP — производство металлических входных дверей

ERP для завода входных дверей: две связанные воронки (**Отдел продаж ⇄ Завод**), конфигуратор
двери с расчётом цены и себестоимости, склад с автоматическим списанием и возвратом материалов,
планшет цеха, сдельная зарплата и публичная страница заказа по QR-коду.

> Единственный документ о системе: что она делает, как устроена, какие правила зашиты и почему
> именно так. Всё остальное — в коде и в комментариях к нему.
>
> Состояние на 20 сентября 2026: 504 теста, проходят на SQLite и на MySQL.

---

## Что это

Одно приложение для продаж, завода, финансов, кадров и клиента:

| Кто | Где работает | Что делает |
|---|---|---|
| **Отдел продаж** | `/admin` | ведёт сделки по воронке: заявка → замер → договор → передача на завод → отгрузка → закрытие |
| **Завод** | `/admin` (начальник производства) и планшет `/shop` (рабочие, вход по коду) | выполняет наряды по 13 этапам цеха, получает сдельную оплату за каждый закрытый этап |
| **Финансы** | `/admin`, группа «Финансы» | счета, поступления, расходы, касса и банк, долги, зарплата, бонусы |
| **Кадры** | `/admin` | сотрудники, ведомости, бонусы |
| **Директор** | `/admin` | всё, плюс настройки: роли и доступы, воронки, прайс, экран цеха, финансы |
| **Клиент** | `/track/{hash}` по QR | смотрит статус своего заказа без входа в систему |

Ключевая идея — **две связанные воронки**. Сделка продаж, дойдя до этапа «Передано в производство», сама создаёт наряд в воронке завода и списывает материалы со склада. Когда завод закрывает этап с флагом «завершает производство» («ОТК и упаковка»), сделка продаж сама становится «Готово к отгрузке». Пока наряд в цеху, менеджер сделку двигать не может — её ведёт завод.

Вторая идея — **всё, что можно, настраивается из панели, а не в коде**: этапы воронок и их обязательные поля, прайс, права ролей, процент бонуса. Это сделано, чтобы владелец менял систему сам.

---

## Стек и запуск

- PHP 8.5, Laravel 12, Filament v4 (панель), Livewire 3, Tailwind 4. Внешних библиотек для графиков и календарей нет: QR и диаграммы — inline-SVG/CSS.
- База: SQLite для разработки (`database/database.sqlite`), **MySQL 8+ в бою** (hoster.kz). SQL в коде не привязан к движку; весь набор тестов проходит на обоих: `php artisan test` (SQLite в памяти) и `composer test:mysql` (локальный MySQL, база `gravit_test`).
- Файлы (договоры, чеки, аватары) — **закрытый диск** `storage/app/private`: наружу отдаются только по подписанной ссылке на 30 минут (`app/Support/Uploads/PrivateFiles.php`), без входа файл не открыть. Диск задан один раз для всех полей загрузки в `AppServiceProvider`.
- Проверка кода: PHPUnit (504 теста), Larastan уровень 5 (48 известных ошибок про `$this->record` в страницах Filament — это базовый долг, не увеличивать), Pint.
- Сборка фронтенда `public/build` **хранится в git**: на боевом сервере с 1 ГБ памяти Node не ставится.

Запуск локально: `composer install && npm install && npm run build && php artisan migrate --seed && php artisan serve --port=8080`.

Демо-доступы после `--seed` (пароль `password`): `admin@gravit.kz` (директор), `manager@`, `accountant@`, `hr@`, `surveyor@`, `master@` (начальник производства), `worker@`. Код планшета цеха — **Настройки → Экран цеха**.

Код — в приватном репозитории `github.com/baproger/gravitdoors.kz`, ветка `main`. **Бой — виртуальный хостинг hoster.kz с Plesk** (тариф «Турбо-1»: 15 ГБ, 1 ГБ, 2 vCPU, PHP 8.3/8.4, без root): Plesk → Git тянет `main` по deploy-ключу в каталог `gravit`, веб-корень `gravit/public`, действие при развёртывании `bash deploy/plesk-deploy.sh`, планировщик раз в минуту `artisan schedule:run`, первый запуск `php artisan gravit:install`. Пошагово — раздел «Развёртывание» ниже. Зависимости закреплены под PHP 8.3+ (`config.platform.php`), на сервере выбирается 8.4.

**Важно про базу.** `migrate:fresh` стирает всё, включая настроенные руками этапы и права. Один раз это уже случилось: 13 этапов цеха пришлось восстанавливать. Поэтому боевая цепочка цеха теперь в сидере `GravitFactoryStagesSeeder` и накатывается последней при любом `--seed`. На боевом сервере `migrate:fresh` не запускать; для проверок есть тесты в памяти и `gravit:backup`.

### Технологии
| Слой | Что использовано |
|---|---|
| Backend | Laravel 12, PHP 8.3+ |
| Админка | Filament v4 (на Livewire v3) |
| Экран цеха | Livewire v3, отдельный публичный маршрут |
| Стили | Tailwind CSS v4 + Vite |
| БД | SQLite из коробки, MySQL/PostgreSQL — сменой `DB_*` в `.env` |
| QR | `bacon/bacon-qr-code`, инлайн-SVG без файлов |

> **Про версии.** В ТЗ значились Laravel 11 + Filament v3. Взяты Laravel 12 + Filament v4:
> Laravel 11 официально поддерживает PHP до 8.4, а на машине PHP 8.5 — установка ломалась бы
> на `composer install`. API Filament v4 отличается неймспейсами (`Filament\Schemas\*` для
> layout-компонентов), вся логика ТЗ реализована без изменений.

### Быстрый старт
```bash
composer install
cp .env.example .env && php artisan key:generate
touch database/database.sqlite
php artisan migrate --seed
npm install && npm run build
php artisan serve
```

> **Это доступы для разработки, и только.** Репозиторий открыт, поэтому пароль
> ниже знает каждый, кто умеет читать. Они появляются от `php artisan migrate --seed`
> вместе с демо-сделками и на боевом сервере оказаться не должны: там директор
> заводится через `php artisan gravit:install`, который требует свой пароль
> не короче восьми знаков, и демо-данных не создаёт. Если база всё же приехала
> с разработки — утренняя проверка `gravit:server-check` найдёт такие записи
> и напишет директору в колокольчик.

| Что | Где | Доступ |
|---|---|---|
| Админка | `/admin` | `admin@gravit.kz` / `password` |
| Менеджер | `/admin` | `manager@gravit.kz` / `password` |
| Бухгалтер-финансист | `/admin` | `accountant@gravit.kz` / `password` |
| HR-директор | `/admin` | `hr@gravit.kz` / `password` |
| Замерщик | `/admin` | `surveyor@gravit.kz` / `password` |
| Рабочий | `/admin`, `/shop` | `worker@gravit.kz` / `password` |
| Мастер цеха | `/admin` | `master@gravit.kz` / `password` |
| Планшет цеха | `/shop` | 6-значный код из **Настройки → Экран цеха** |
| Страница клиента | `/track/{hash}` | ссылка/QR из карточки сделки |

---

## Модель данных

```
users ─┬─ deals (сделка продаж) ─────┬── deals (наряд завода, parent_deal_id)
       │      │                       │      └── production_logs (этап цеха: кто, когда, сдельная оплата)
       │      ├── door_configurations (двери заказа)
       │      ├── deal_payments (платежи с чеками) ──→ cash_movements
       │      ├── deal_events (история: кто, из какого отдела, что сделал)
       │      ├── deal_stage_visits (журнал заходов на этапы)
       │      └── bonuses (автобонус менеджеру, deal_id)
       │
       ├─ factory_stages (этапы ОБЕИХ воронок: pipeline_type = sales | factory)
       ├─ door_options (прайс конфигуратора по группам)
       ├─ material_stocks + stock_movements (склад и движения, в т. ч. факт по наряду)
       │
       ├─ expenses (расходы: категория, статус, чек) ──→ cash_movements
       ├─ cash_accounts + cash_movements (касса и банк: журнал, остаток считается)
       ├─ debts + debt_payments (долги компании; платёж = расход + движение)
       ├─ salary_sheets + salary_payments (ведомость за месяц; выплата = расход + движение)
       ├─ bonuses (бонусы сотрудникам, ручные и автоматические)
       │
       ├─ roles (справочник ролей: название, цвет, галочки поведения)
       ├─ role_permissions (отличия матрицы прав от рекомендованной)
       └─ settings (код планшета, процент бонуса, автоутверждение)
```

**Сделка и наряд — одна таблица `deals`.** Наряд — это отдельная строка с `pipeline_type = factory` и ссылкой `parent_deal_id` на сделку продаж. В наряд копируются сумма, себестоимость, клиент, телефон, адрес, менеджер и срок; своих дверей у наряда нет, он берёт позиции у сделки. Цех этих сумм не видит только потому, что право «Суммы в карточках» у него на «Нет» — данные в строке есть.

**Этапы обеих воронок — одна таблица `factory_stages`.** Колонка `pipeline_type` разделяет воронки. Флаги автоматизации (`triggers_production` у продаж, `completes_production` у завода), норматив часов, сдельная расценка, обязательные поля — в этапе, настраиваются в админке.

**Замер живёт в сделке двумя разными вещами.** `measured_at` — это *план*: когда замерщик поедет, ставит менеджер. `measurement_height`, `measurement_width`, `measurement_comment`, `measurement_done_at`, `measurement_by_id` — *факт*: что замерщик привёз с выезда. Отдельной таблицы замеров нет: на сделку он один.

**Позиция прайса записана в дверях парой «группа + код».** Поэтому код и группу нельзя менять после создания, а позицию из заказа нельзя удалить — только снять с продажи.

**Себестоимость: материалы на дверь, труд на заказ.** Материалы считаются по каждой позиции и умножаются на количество, а сдельная оплата цеха прибавляется **один раз на заказ** (`DealPriceSummary::estimatedCost()`). Иначе заказ на три двери показывал бы тройную себестоимость труда при одинарной выплате цеху: наряд проходит этап один раз, и `production_logs` заводится на заказ, а не на изделие.

**Деньги — одна цепочка.** Платёж по сделке и подтверждённый расход порождают движение по счёту (`CashLedger`); платёж по долгу и выплата зарплаты порождают расход, а он — движение. Остаток счёта не хранится, а считается по журналу. Так нельзя рассинхронизировать кассу правкой поля.

Статусы, роли, категории, права — PHP-enum'ы в `app/Enums` (`DealStatus`, `UserRole`, `Permission`, `AccessLevel`, `ExpenseCategory`, `StageRequirement`, `Department` и др.).

### Схема данных
| Таблица | Назначение |
|---|---|
| `deals` | Сделки и наряды. Карточка клиента (тип, компания, БИН, два телефона, e-mail, город, источник), договор, условия оплаты (предоплата, способ, доставка, монтаж), `status_id` (int-enum `DealStatus`), `pipeline_type`, `current_stage_id`, `parent_deal_id`, `qr_code_hash` |
| `door_configurations` | **Позиции сделки**: одна строка — одна дверь. Габариты, сторона открывания, металл, панели МДФ, замок, утеплитель, покрытие, `additional_options` (json), `price_breakdown` (json) |
| `factory_stages` | Этапы **обеих** воронок (`pipeline_type`), порядок, `estimated_hours`, `operation_cost`, флаги автоматизации |
| `door_options` | Прайс конфигуратора: группа, цена, тип цены (фикс / за м² / за м.п.), привязка к складу и расход |
| `material_stocks` | Склад: остаток, минимальный порог, цена закупа |
| `stock_movements` | Приход, списание под наряд и возврат. Остаток `material_stocks.quantity` производен от этих строк |
| `production_logs` | Прохождение наряда по этапам: исполнитель, факт/норматив времени, зафиксированная сдельная выплата |
| `settings` | Настройки, меняемые владельцем: код планшета цеха |
| `users` | Сотрудники: роль, доступ, фото, оклад, дата приёма и день рождения |
| `deal_payments` | Платежи клиента: сумма, способ, дата, чек, кто принял |
| `deal_events` | История сделки: автор, отдел на момент действия, тип события, описание и разбор правок |

Одна сделка содержит **несколько дверей**: заказ на входную и тамбурную дверь — это одна
сделка с двумя позициями, а не две сделки, потерявшие связь в отчётах. Цена сделки —
сумма позиций, списание материалов — по всем позициям с агрегацией по номенклатуре
(одна строка расхода на материал, а не десяток мелких).

`status_id` — намеренно int-backed enum: колонка названа как в ТЗ, но в коде это
перечисление `DealStatus`. Шаг между значениями = 10, чтобы новые статусы вставлялись
без миграции данных.

---

## Роли и доступы

Роли лежат в справочнике `roles` и правятся из панели. Семь базовых: **Директор** (`admin`), **Менеджер продаж**, **Бухгалтер-финансист**, **HR-директор**, **Начальник производства** (`master`), **Рабочий цеха**, **Замерщик** — их можно переименовать и перекрасить, но не удалить: на их кодах держатся автоматизации и уже заведённые сотрудники. Свои роли («Кладовщик») директор заводит кнопкой **«Создать роль»** в «Настройки → Роли и доступы»: название и роль-образец, с которой копируются все 32 права. Роль назначается в карточке сотрудника.

Кроме прав у роли две галочки поведения: **«Ездит на замеры»** (уведомления о выездах и кнопка «Замерял») и **«Работает в цеху»** (сдельная оплата за закрытый этап, виден в выборе исполнителя на планшете). Раньше это решал `match` по перечню в коде.

**Право «Роли и доступы» не выдаётся никому, кроме директора** — ни копированием, ни руками в матрице: иначе роль смогла бы выписать себе всё остальное.

Что роль видит и может — не в коде, а в матрице **Настройки → Роли и доступы** (`/admin/access`, только директор). У каждой роли уровень по каждому из 32 прав:

| Уровень | Что значит |
|---|---|
| **Нет** | пункт меню скрыт, прямая ссылка → 403 |
| **Чтение** | видно, но без создания, правки, перетаскивания и действий; карточка сделки открывается отдельной страницей просмотра |
| **Только свои** | видно и можно менять лишь записи, где сотрудник — ответственный; чужая сделка отдаёт 404 |
| **Полный** | без ограничений |

Рекомендованная матрица (`—` нет · `Ч` чтение · `С` только свои · `П` полный) — она же значения по умолчанию, в базе хранятся только отличия:

| Раздел | Менеджер | Директор | Бухгалтер | HR | Производство | Рабочий | Замерщик |
|---|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| Воронка продаж | С | П | Ч | — | — | — | — |
| Воронка завода | Ч | П | Ч | Ч | П | Ч | — |
| Просроченные | С | П | Ч | Ч | П | — | — |
| Сделки и наряды | С | П | П | — | Ч | Ч | — |
| Склад и движения | Ч | П | П | — | П | — | — |
| Моя зарплата | П | П | П | П | П | П | П |
| Финансы: обзор, счета, поступления, расходы, касса, долги | — | П | П | — | — | — | — |
| Зарплата цеха, ведомость, бонусы | — | П | П | П | — | — | — |
| Настройки: сотрудники | — | П | Ч | П | — | — | — |
| Настройки: экран цеха | — | П | — | — | П | — | — |
| Настройки: воронки, прайс, справочники, роли | — | П | — | — | — | — | — |

Отдельные полномочия внутри экранов: удаление сделок (только директор), отказ от сделки (менеджер — своей), блокировка отгрузки (финансы), сводные суммы воронки (директор, бухгалтер), суммы на карточках, **подтверждение расходов, бонусов и выплат** (отдельное право: сняв его, директор оставляет проверку за собой, а раздел остаётся рабочим), оклады и ставки (финансы, кадры), факт материалов (цех).

Как устроено: `Permission` и `AccessLevel` — перечни, `App\Services\AccessControl` — реестр (кэш + память запроса + отличия в `role_permissions`), политики в `app/Policies` спрашивают уровень у него. Прямых сравнений с ролью в коде нет. Инфопанель, списки, канбаны, поиск и прямые ссылки фильтруются одним scope `Deal::visibleTo()`.

**Директор всегда имеет полный доступ** (`Gate::before`), его строку в матрице изменить нельзя — иначе систему можно запереть без хозяина. Поэтому запреты «нельзя удалить подтверждённое / занятое / с нарядом в цеху» живут в моделях (`canBeDeleted()`) и в действиях, а не только в политиках.

Приёмка по ролям автоматизирована: `RoleMatrixAcceptanceTest` обходит все разделы под каждой ролью.

**Защита снаружи.** Вход: 5 попыток в минуту, второй фактор через приложение-аутентификатор (профиль → «Двухфакторная аутентификация», коды восстановления). Файлы — закрытый диск и подписанные ссылки с адреса `/files`, а не `/storage`: последний закрыт в `public/.htaccess`, и Apache гасил бы подписанную ссылку раньше, чем запрос дойдёт до PHP (`PrivateFileRouteTest` сверяет адреса всех маршрутов с правилами веб-корня). Заголовки безопасности и HSTS на каждом ответе, куки только по HTTPS. Политика содержимого (`Content-Security-Policy`) закрывает подмену `<base>`, отправку формы на чужой сайт, встраивание в чужой iframe и плагины; `script-src` в неё намеренно не входит — Alpine, на котором держится панель, требует `unsafe-eval`, а с ним это правило не защищает ни от чего и ломается на каждом обновлении Filament. Панель не обращается ни к одному чужому серверу: шрифт свой, кружки с инициалами рисуются у себя (по умолчанию Filament отправлял бы имена сотрудников на `ui-avatars.com`), и `test_pages_load_no_resources_from_other_sites` не даст вернуть внешнюю ссылку. Публичные адреса `/track` и `/shop` под лимитом запросов и кодом. Веб-корень отдаёт только `public/`: если код приложения когда-нибудь окажется внутри него (неверный путь развёртывания), `public/.htaccess` всё равно закроет `config`, `database`, `storage` и `.env`, а `plesk-deploy.sh` остановится до миграций и объяснит, что поправить. Все действия проверяются политиками на сервере; сумму сделки, статус и этап руками не поменять. Проверка: `tests/Feature/SecurityHardeningTest.php` — в том числе лимит входа: он даёт не наш код, а Filament, и обновление панели могло бы снять его молча.

### Права доступа
Роли лежат в справочнике `roles` (`App\Models\Role`), а `users.role` хранит код роли. Плюс
**редактируемая матрица прав**: у каждой роли есть уровень доступа (нет / чтение / только свои /
полный) по каждому разделу и полномочию. Матрица правится в **Настройки → Роли и доступы**
(`/admin/access`) и действует сразу.

Семь базовых ролей помечены `is_system`: их можно переименовать, перекрасить и менять им права,
но не удалить — на их кодах держатся автоматизации и заведённые сотрудники. Свои роли директор
заводит там же кнопкой **«Создать роль»**: указывает название и роль-образец, с которой
копируются все 32 права. Кроме прав у роли две галочки поведения — **«Ездит на замеры»**
(уведомления о выездах и кнопка «Замерял») и **«Работает в цеху»** (сдельная оплата за закрытый
этап, виден в выборе исполнителя на планшете); раньше это решал `match` по перечню в коде.

Право **«Роли и доступы» не выдаётся никакой роли, кроме директора** — ни копированием, ни
руками в матрице: иначе роль смогла бы выписать себе все остальные права.

- `App\Enums\Permission` — что вообще можно разрешать (разделы меню и полномочия внутри экранов).
- `App\Enums\UserRole` — коды семи встроенных ролей; ими заполняется справочник при миграции.
- `App\Enums\AccessLevel` — четыре уровня, упорядоченные по силе.
- `App\Services\AccessControl` — единственный источник истины: кэш, отличия в `role_permissions`,
  рекомендованные значения из ТЗ как значения по умолчанию.
- `app/Policies/*` спрашивают уровень у реестра; прямых сравнений с ролью в коде нет.

Директор (`admin`) проходит любую политику через `Gate::before`, и его строку в матрице изменить
нельзя. Поэтому запреты вида «нельзя удалить подтверждённое» живут в моделях и в действиях.

| | admin | manager | surveyor | master | worker |
|---|:---:|:---:|:---:|:---:|:---:|
| Сделки продаж | ✓ | ✓ | — | — | — |
| Производственные наряды | ✓ | ✓ | — | ✓ | ✓ (просмотр) |
| Канбан завода, закрытие этапов | ✓ | — | — | ✓ | канбан только просмотр |
| Суммы и маржа | ✓ | ✓ | — | — | — |
| Прайс конфигуратора | ✓ | только чтение | — | — | — |
| Склад: остатки / приход | ✓ | ✓ | — | ✓ | — |
| Просроченные | ✓ | ✓ | — | ✓ | ✓ (без ссылок на карточки) |
| Уведомления о замерах | — | — | ✓ | ✓ | — |
| Этапы воронок, сотрудники | ✓ | — | — | — | — |
| Удаление сделок | ✓ (без наряда в цеху) | — | — | — | — |
| Отмена сделки | ✓ | ✓ | — | — | — |

Ограничение по воронке стоит на запросе (`DealResource::getEloquentQuery`), поэтому действует
и в списке, и в глобальном поиске, и по прямой ссылке. Перетаскивание карточки на канбане
перепроверяется политикой на сервере: разметка ничего не решает.
Отключённый сотрудник (`is_active = false`) не войдёт в панель. Переключатель «Доступ» в списке
сотрудников работает только у администратора: колонка-переключатель Filament минует политику,
поэтому запрет стоит на ней самой.

Администратор проходит любую политику, поэтому правила «нельзя удалить сделку с нарядом в цеху»,
«живой наряд не удаляется, а отменяется» и «материал с движениями не удаляется» живут в моделях
(`canBeDeleted()`) и проверяются действиями удаления и наблюдателем, а не только политикой.

---

## Как течёт сделка

1. **Заявка.** Менеджер заводит сделку: клиент (физлицо/компания, БИН, телефоны, адрес, источник), двери (линейка, модель, размеры, металл, МДФ, замок, утеплитель, покрытие, опции), срок сдачи (обязателен). Цена считается сама из прайса: `габариты × цена за м²` + фиксированные опции + сборка + доставка + монтаж; руками сумму не поменять.
2. **Замер.** Дата и время замера → уведомление замерщику и начальнику производства (без ссылки на карточку: им она не открывается). Утром в 08:30 замерщики и ответственный менеджер получают список выездов на день с временем, адресом и телефоном. День прошёл, а сделка не дошла до договора — «замер просрочен», красным везде, и утром об этом уведомляются директор и ответственный менеджер. С выезда замерщик возвращается и нажимает **«Замерял»** в плитке «Замеры сегодня» на инфопанели (там же висят и просроченные выезды): высота, ширина, комментарий ложатся в сделку блоком «Результат замера» на вкладке «Клиент» и одной записью «Замер» в историю, ответственный менеджер получает уведомление со ссылкой. Воронку замерщик не трогает — этап двигает менеджер.
3. **Договор.** Номер, дата, файл договора. Платежи — каждый с чеком; сумма платежей = предоплата, и она не больше суммы сделки (проверяется на сервере).
4. **Передача на завод.** Вход на этап с `triggers_production`: создаётся наряд, списываются материалы по расходу каждой опции, пишется история. Сделка заблокирована до конца производства. Готовый наряд тоже считается «передано»: второй раз на завод сделка не уходит.
5. **Цех — 13 этапов.** Согласование дизайна → создание чертежа → раскрой материала → лазерная резка → гибка листа → сварка и сборка → полимерная покраска → МДФ и стекло → фурнитура и замки → сборка заказа → **ОТК и упаковка** (завершает наряд) → доставка → монтаж. Рабочий на планшете нажимает «Взял» / «Готово» (без выбранного исполнителя «Готово» не срабатывает); начальник производства двигает наряды на канбане и отмечает факт материалов сверх плана. Сдельная оплата за этап — тому, кто взял его в работу, иначе тому, кто закрыл, если это цех; директор, закрывший этап с канбана, оплату не получает. Расценка фиксируется в момент закрытия.
6. **Готово к отгрузке.** Завод закрыл этап с `completes_production` → сделка продаж сама переходит дальше со статусом «Готово к отгрузке». Финансы могут поставить **блокировку отгрузки** с причиной: цех работает, но дальше передачи в цех сделка не пойдёт, пока долг не закрыт.
7. **Доставка и монтаж → Сделка закрыта.** Закрыть можно только при полной оплате. Завершающий этап ставит «Завершена» и начисляет **автобонус менеджеру** — процент от суммы сделки (по умолчанию 2 %, ставка в «Настройки → Финансы», у сотрудника может быть своя), один раз на сделку, в месяц закрытия, на утверждение или сразу — по настройке.

Отмена наряда: материалы возвращаются на склад по сальдо «списано − возвращено», сделка возвращается на шаг назад и её можно передать заново. Отмена сделки: кнопкой в шапке карточки, живой наряд отменяется вместе с ней.

Открытый вопрос владельцу: этапы «Доставка» и «Монтаж» стоят в воронке завода **после** «ОТК и упаковка», но наряд закрывается на ОТК, и до них не доходит. Либо перенести флаг завершения на «Монтаж» (одним кликом в «Этапы воронок»), либо убрать эти два этапа из цеха — доставку и монтаж тогда ведут продажи своим этапом.

### Архитектура воронок
Обе воронки живут в **одной** таблице `deals`, различаясь колонкой `pipeline_type`:
производственный наряд — это та же сделка с `pipeline_type = factory` и ссылкой
`parent_deal_id` на сделку продаж. Связка «продажи ⇄ завод» остаётся одним JOIN,
а не синхронизацией двух сущностей, которые рано или поздно разойдутся в данных.

Этапы обеих воронок настраиваются в админке (**Настройки → Этапы воронок**): название,
порядок перетаскиванием, цвет, норматив часов, сдельная расценка. Новый этап не требует
правки кода.

#### Правила автоматизации

Живут в `app/Services/DoorProductionService.php` и **только** там — ни канбан, ни ресурсы
Filament, ни планшет цеха не двигают сделки напрямую, иначе перетаскивание карточки, кнопка
в карточке и кнопка на планшете разошлись бы в поведении.

**1. Продажи → Завод.** Сделка входит на этап с флагом `triggers_production`:
создаётся наряд на первом этапе цеха, со склада списываются материалы всех позиций,
статус сделки → «Передано в производство». Идемпотентно: второй наряд не появится.

**2. Завод → Продажи.** Завершается этап с флагом `completes_production` («ОТК и Упаковка»):
наряд закрывается, сделка продаж переходит в **«Готово к отгрузке»** и встаёт на следующий
этап воронки.

**3. Отмена наряда** возвращает материалы на склад по сальдо «списано − возвращено»
и возвращает сделку клиента в работу. Повторная отмена ничего не задваивает.

#### Регламент воронки

Сделка идёт по этапам подряд — «сразу закрыть» её нельзя. Для входа на этап должны быть
заполнены обязательные поля; набор настраивается в админке (**Настройки → Этапы воронок →
Что обязательно заполнить**), из коробки:

| Этап | Что требуется |
|---|---|
| Замер и расчёт | телефон, адрес объекта, город, ответственный |
| Договор и предоплата | дата замера, хотя бы одна дверь, срок сдачи, реквизиты компании |
| Передано в производство | номер и дата договора, загруженный договор, предоплата |
| Сделка закрыта | оплачено полностью |

Назад сделку можно вернуть всегда: откат нужен именно тогда, когда данные оказались
неверными, и запирать его нечем.

#### Замеры

Когда менеджер ставит дату **и время** замера, замерщики (роли «Замерщик» и «Мастер цеха»)
получают уведомление «Замер 22.09.2026 в 14:30» с адресом и телефоном клиента. Перенос даты
уведомляет повторно, правка других полей — нет. Утром в 08:30 (`gravit:daily-check`) замерщики
и ответственный менеджер получают список выездов на день; если день замера прошёл, а сделка
не дошла до договора, о просрочке узнают директор и ответственный менеджер, а сделка помечена
красным в карточке, на канбане и в «Просроченных».

С выезда замерщик возвращается и нажимает **«Замерял»** у нужного выезда в плитке «Замеры
сегодня» на инфопанели — там же висят и просроченные замеры, потому что страница
«Просроченные» ему не открывается. В модалке: высота, ширина и комментарий. Они ложатся в
сделку (`measurement_height`, `measurement_width`, `measurement_comment`, `measurement_done_at`,
`measurement_by_id`), показываются блоком «Результат замера» на вкладке «Клиент» и одной
записью «Замер» в истории, а ответственный менеджер получает уведомление со ссылкой на
карточку. Этап замерщик не двигает: право `DealPolicy::measure()` даёт только запись замера,
воронка остаётся за менеджером.

```
Отдел продаж:  Новая заявка → Замер → Договор → [Передано в производство] → Готово к отгрузке → Доставка → Закрыта
                                                          │ triggers_production        ▲
                                                          ▼                            │ completes_production
Завод:              Раскрой → Сварка → Покраска → Утепление/МДФ → Фурнитура → [ОТК и Упаковка]
```

### Время на этапе
Каждый заход сделки на этап пишется в `deal_stage_visits` (вошла / ушла). Таймер «⏱ N ч» на
канбане и в полосе этапов показывает **накопленное** время за все заходы: вернули сделку на
этап — прежние часы не пропадают, у таймера появляется «2-й заход» (на канбане — «↺»). У пройденных
этапов на полосе видно, сколько каждый занял. Норматив этапа тоже считается по накопленному времени.
Журнал ведёт наблюдатель сделки, поэтому все пути смены этапа (канбан, карточка, удаление этапа
воронки) попадают в него одинаково; для уже существующих сделок журнал восстановлен из истории смен этапов командой
`php artisan gravit:rebuild-stage-visits` (можно `--deal=ID`): история хранила каждую смену с точным
временем и названиями «откуда → куда», так что заходы собраны без домыслов.

### Просрочка
Срок сдачи — обязательное поле сделки. Просроченная дата горит красным везде: карточка на канбане
(красная кромка и «просрочено N дн.»), строка в списке, полоса этапов в карточке, наряд на планшете
цеха. Таймер этапа краснеет, когда сделка стоит на этапе дольше норматива (`estimated_hours`).

**Замер** просрочен, когда дата замера прошла, а сделка ещё не дошла до этапа, для которого
замер обязателен («Договор и предоплата» из коробки — настраивается в этапах воронки). После
этого этапа дата в прошлом — просто история, не просрочка. Метка «Замер просрочен N дн.» —
на канбане, в карточке (красная полоса и подсказка у поля), в списке и в «Просроченных».

`/admin/overdue-deals` — страница «Просроченные» с красным счётчиком в меню. Три вкладки:
«Срок сдачи прошёл», «Замер не проведён» и «Застряли на этапе» (дольше норматива); везде
самая большая просрочка сверху. Закрытые сделки просроченными не считаются. Цех видит
только свои наряды.

### Сделка ждёт завод
Пока у сделки продаж есть незавершённый наряд, по воронке её ведёт производство, а не менеджер.
Ручной переход **в любую сторону** запрещён — полосой этапов, стрелками и перетаскиванием на
канбане. Вперёд — потому что сделка обгоняла бы цех («Готово к отгрузке», а дверь ещё на
покраске). Назад — потому что сделка отрывалась бы от живого наряда.

- На канбане у такой карточки нет стрелок и перетаскивания, есть метка «Ждёт завод: <этап цеха>».
- В карточке сделки этапы заблокированы, под полосой — какой наряд и на каком он этапе.
- Дальше сделку переводит сам завод: закрытие «ОТК и Упаковка» ставит её на следующий этап
  после «Передано в производство» со статусом «Готово к отгрузке».
- Если заказ отменили, сначала отменяется наряд («Отменить наряд» в списке сделок) — после
  этого сделка снова двигается вручную. Кнопка «Отменить сделку» в карточке делает это одним
  действием: отменяет живой наряд, возвращает материалы и закрывает сделку статусом «Отменена».
- Готовый наряд считается «уже передано»: сделку, возвращённую назад и снова доведённую до
  «Передано в производство», второй раз на завод не отправят и склад не спишут.
- Статус сделки следует за этапом и в карточке только для чтения: до передачи — «В работе»,
  на этапах после завода — «Готово к отгрузке», завершающий этап — «Завершена».

Поле «Этап» в карточке сделки — **только для чтения**. Раньше оно писало этап напрямую, в обход
порядка этапов, обязательных полей и завода. Новая сделка начинается с первого этапа воронки.

---

## Правила, которые нельзя обойти

**Воронка**
- Вперёд — только на соседний этап; назад — свободно.
- Вход на этап требует обязательных полей (настраиваются в «Этапах воронок»).
- Пока наряд в цеху, сделку продаж не сдвинуть ни вперёд, ни назад.
- Блокировка отгрузки не пускает сделку дальше этапа передачи в цех; ставится и снимается только с правом «Отметка оплаты», с обязательной причиной, пишется в историю.
- Статус сделки следует за этапом и в карточке только для чтения; отмена — отдельной кнопкой.
- Поле «Этап» в карточке — только для чтения; этап меняется полосой этапов или на канбане.
- Автоматика в воронке — ровно один этап; выключить нельзя, только передать другому. Такой этап не удаляется и не скрывается.
- Этап со сделками удаляется только с переносом сделок (не на завершающий); этап цеха с работами — не удаляется (по нему считается зарплата).
- Сделку с нарядом в цеху не удалить; живой наряд не удаляется, а отменяется; материал с движениями или позициями прайса не удаляется. Сделка без ответственного видна всем на уровне «только свои», чтобы не потеряться.

**Прайс**
- Снять с продажи ≠ удалить: снятая позиция не предлагается новым дверям, но в старых заказах считается и списывается.
- Позицию, выбранную хоть в одной двери, удалить нельзя. Вариант по умолчанию в группе один.

**Деньги**
- Платёж без чека не сохраняется; сумма платежей не больше суммы сделки. Все загрузки (чеки, договоры, аватары) сжимаются при сохранении: фото до 1600 px / JPEG 82 %, PDF через Ghostscript, если он есть (`app/Support/Uploads/UploadCompressor.php`, подключён ко всем полям загрузки в `AppServiceProvider`). Если спецификацию урезали ниже внесённого, карточка предупреждает.
- Расход попадает в сводку и списывается со счёта только после подтверждения; для всех категорий, кроме зарплаты и налогов, нужен чек. Подтверждённый расход не удаляется — только отклоняется с причиной.
- Ведомость: удержания правятся только в черновике; утверждённая зафиксирована (повышение оклада не меняет прошлый месяц); выплата не больше остатка и только по утверждённой; оклад пропорционален дням, если принят внутри месяца.
- Долг: платёж не больше остатка, с остатком не закрывается, с платежами не удаляется.
- Остаток кассы и банка считается по журналу; руками — только перевод между счетами и корректировка с причиной.
- Бонус утверждённый не удаляется, только снимается с утверждения; автобонус — один на сделку.
- Списание материалов при повторной передаче не задваивается; наряд по сделке один.
- Закрытый наряд не берётся в работу, не закрывается и не отменяется повторно.

**Время**
- Время на этапе копится за все заходы (`deal_stage_visits`); норматив сравнивается с суммой.
- Просрочка — только у открытых. Ежедневная проверка и «Просроченные» считают её одинаково. По сроку сдачи продажам показываются сделки, а не их наряды.

### Калькулятор
`app/Services/DoorPriceCalculator.php`. Формула — «габариты × металл + панели МДФ + фурнитура»:

- **за м²** — цена умножается на площадь полотна (металл, МДФ, утеплитель, покрытие);
- **за м.п.** — на периметр коробки (уплотнитель);
- **фикс** — за изделие (замки, доводчик, глазок, броненакладка).

Ни одна цена не зашита в код — всё из `door_options`, правится в админке.
Себестоимость считается из другого источника: материалы (`consumption × price_per_unit`)
плюс сумма сдельных расценок этапов цеха. Расшифровка сохраняется в
`door_configurations.price_breakdown`, поэтому правка прайса задним числом не переписывает
историю уже посчитанных заказов.

Цена сделки — производная: её пересчитывает `DoorProductionService::syncPricing()` при
сохранении карточки, а не набирает менеджер руками.

### Прайс: снятые с продажи позиции
Позиция прайса записана в дверях заказов парой «группа + код». Поэтому:

| Действие | Правило |
|---|---|
| Выключить позицию | значит **снять с продажи**: новым дверям её не предлагают, но там, где уже выбрана, она продолжает считаться в цене и списываться со склада |
| Удалить позицию | только если она не выбрана ни в одной двери, включая заказы в корзине; иначе — снять с продажи |
| Код и группа | после создания не меняются; код создаётся из названия |
| Вариант по умолчанию | один в группе, только у позиции в продаже; у доп. опций его нет |

Раньше выключенная позиция молча выпадала из расчёта: при пересохранении старой сделки её
цена падала, материал не списывался, а форму сделки нельзя было сохранить — выпадающий список
не находил выбранное значение среди вариантов. Теперь выбранная снятая позиция остаётся в списке
с пометкой «снята с продажи».

### Этапы воронок
`/admin/factory-stages` — настройка воронок в духе Битрикс24, только для администратора.

- **Две вкладки** — «Отдел продаж» и «Завод». Сверху полоса «Как видят сотрудники» —
  воронка в том порядке, в каком она будет на канбане, с отметкой ⚡ у этапа автоматики.
- **Этапы строками** в двух группах: «Этапы в работе» и «Завершающие этапы». Порядок —
  перетаскиванием за «⋮⋮» или кнопками ↑ ↓ (на телефоне drag-события не приходят),
  название правится прямо в строке, цвет — по клику на кружок. Сохраняется сразу.
- **«+ Добавить»** в каждой группе. Новый рабочий этап встаёт перед завершающими.
- **Шестерёнка** — редкие настройки: описание, норматив, сдельная оплата (завод),
  обязательные поля (продажи).
- **Меню «⋯»** — назначить автоматику, сделать этап завершающим или рабочим, скрыть, удалить.

Порядок (`order`) и первый этап (`is_initial`) руками не задаются: после каждой правки
`PipelineStageService` расставляет 10, 20, 30… и отмечает первый видимый рабочий этап.
Код этапа генерируется из названия и при переименовании не меняется.

Правила, которые не дают сломать воронку:

| Действие | Правило |
|---|---|
| Удалить этап со сделками | только с выбором, куда перенести; в истории каждой сделки — запись. Раньше сделки молча оставались без этапа |
| Удалить этап цеха с работами | нельзя: по записям цеха считается зарплата; такой этап можно скрыть |
| Удалить или скрыть этап автоматики | нельзя, пока автоматика не назначена другому этапу |
| Автоматика | в воронке ровно один такой этап: включение на одном снимает флаг с остальных |
| Завершающий этап продаж | не может передавать сделку в производство |
| Последний рабочий этап | удалить, скрыть или сделать завершающим нельзя |
| Скрыть этап | только пустой; скрытый этап пропускается в последовательности этапов |
| Название | 2–60 символов, без повторов внутри воронки (без учёта регистра) |

---

## Экраны

**Инфопанель** `/admin` — по ролям: директор и бухгалтер видят всё целиком; у остальных сверху блок **«Моя работа»** только со своими цифрами — рабочий: закрытые им этапы, его сдельная оплата, наряды у него в работе (сводки по всему цеху не видит; начальник производства видит); замерщик и менеджер: замеры сегодня с временем и адресом, просроченные тут же в списке, на неделю, у каждого выезда кнопка «Замерял»; менеджер: свой бонус за период и ссылка на «Мою зарплату» (`DashboardStats::isFullView()`, `seesFactoryOverview()`, `my*()`). Период (сегодня, неделя, месяц, прошлый месяц, квартал, год, всё время, свой отрезок; живёт в адресной строке) и по нему: продажи (новые, средний чек, портфель, просрочка, конверсия), деньги (поступления с трендом, дебиторка, прибыль, расходы по статьям, касса и банк, долги, столбики по месяцам), цех (наряды, закрытые этапы, средний цикл, застрявшие, загрузка по этапам, выработка), склад (что закупать), люди (к выплате, бонусы на утверждении). Блок появляется только у того, кому открыт раздел.

**Продажи:** Воронка продаж (канбан; итог по колонке — только с правом «Сводные суммы») · Сделки и наряды (вкладки «Отдел продаж» / «Завод» — только открытые, «Закрытые» — завершённые и отменённые, «Все, включая закрытые»; поиск по номеру, клиенту, телефону, БИН; фильтры по воронке, статусу, этапу, менеджеру, источнику, сроку сдачи, дате создания и дате переноса на этап; под этапом видно, кто и когда сделку двинул) · Карточка сделки (полоса этапов, вкладки Клиент / Двери / Оплата / Договор, блок «Результат замера», история; для читателей — отдельная страница просмотра; кнопки в шапке по отделам — Отдел продаж / Финансы / Завод / Клиент, «Принять оплату» отдельно с подставленным остатком, та же кнопка на вкладке «Оплата» и в подсказке полосы этапов, когда не хватает только оплаты; все действия — `app/Filament/Actions/DealActions.php`, они же в списке сделок и в «Счетах») · Просроченные (срок сдачи, замер, застряли на этапе) · Клиенты (`/admin/clients`: сделки продаж, свёрнутые по телефону — имя и компания из последней сделки, сколько сделок и в работе, заказал на, должен, последняя сделка; поиск по имени, телефону, компании, БИН; «Сделки» — список по клиенту, «Новая сделка» — карточка заполняется реквизитами из последней сделки). Отдельной таблицы клиентов нет — клиент живёт в сделке, закрытая сделка и есть история клиента.

**Завод:** Воронка завода (на последнем этапе кнопка «Готово ✓») · Планшет цеха `/shop` (крупные карточки, выбор исполнителя, «Взял» / «Готово», без сумм) · Зарплата цеха (сдельно по этапам за месяц).

**Склад:** остатки с минимумом, приход, движения (приход, расход, возврат по наряду, факт сверх плана); фильтры по материалу, периоду, «только по нарядам».

**Финансы:** Обзор `/admin/finance` (ДДС: счета и долги, сумма договоров, дебиторка, поступления, расходы по категориям, результат, склад) · Счета (по сделкам: выставлено, оплачено, остаток; вкладки ожидают / просрочены / оплачены; приём оплаты, напоминание) · Поступления (все платежи, итоги касса/банк) · Расходы (категории, чек, на проверке / оплачено / отклонено) · Касса и банк (счета, журнал, переводы, корректировки) · Задолженности (кому должны, оплата долга) · Зарплата (ведомость: оклад + сдельно + бонусы − удержания; сформировать, утвердить, выплатить) · Бонусы · Моя зарплата (у каждого только своё).

**Настройки (директор):** Роли и доступы · Этапы воронок (конструктор: перетаскивание, цвет, норматив, расценка, обязательные поля, автоматика) · Прайс конфигуратора · Сотрудники (карточка с показателями, оклад и бонусный % — по праву «Оклады и ставки») · Экран цеха (код планшета) · Финансы (процент бонуса, автоутверждение).

**Вход** `/admin/login`: слева витрина на синем градиенте с чертёжной сеткой — что система умеет и для кого она; справа форма. Блоки входят лесенкой по 70 мс сверху вниз, форма раньше плиток — за ней пришли; фоновое свечение еле заметно плывёт. Движение только `transform` и `opacity`, при «меньше движения» остаётся проявление без поездок. Оформление своё (`App\Filament\Auth\Login` + `resources/views/filament/auth/login.blade.php`), логика входа целиком филаментовская: лимит попыток, второй фактор, коды восстановления.

**Профиль** `/admin/profile`: своё имя, контакты, фото, пароль. **Клиент** `/track/{hash}`: статус, позиции, сумма, QR.

Везде: календарь с выбором месяца списком и года отдельным полем; фильтры «за период» и «за месяц» на каждой таблице; компактный «Переключить столбцы».

**По расписанию:** 03:00 `gravit:backup` (снимок базы — 14 копий, архив файлов — 3; архив пропускается, если диску останется меньше 1,5 ГБ), 08:00 `gravit:server-check` (диск, память, свежесть бэкапа — директору), 08:30 `gravit:daily-check` (материалы ниже минимума, наряды дольше норматива — тем, кому открыт раздел; замеры на сегодня — замерщикам и ответственному менеджеру; просроченные замеры — директору и ответственному менеджеру).

### Карточка сделки
Собрана как в amoCRM — чтобы менеджер вводил заказ в одном месте, не прыгая по разделам:

- **Полоса этапов сверху.** Клик двигает сделку через `DoorProductionService` — та же
  автоматика, что и на канбане. Вперёд открыт только соседний этап: воронку нельзя
  проскочить. Если для перехода не хватает данных, этап подсвечен оранжевым, а под полосой
  перечислено, чего именно и где. Если не хватает только оплаты, прямо в подсказке стоит
  кнопка «Принять оплату — остаток …». Права проверяются на сервере.
- **Кнопки в шапке — по отделам.** «Принять оплату» отдельно, остальное в группах
  **Отдел продаж** (передать в производство, пересчитать цену, отменить сделку),
  **Финансы** (заблокировать / снять блокировку отгрузки, напомнить об оплате),
  **Завод** (завершить этап, факт материалов, отменить наряд), **Клиент** (страница клиента,
  QR). Каждый видит только свою группу; пустые группы скрыты. Те же кнопки — в списке сделок
  и на странице «Счета»: одно определение (`App\Filament\Actions\DealActions`), одни права.
- **Вкладки слева** — «Клиент», «Двери», «Оплата», «Договор». Полные данные заказчика:
  физлицо или компания (реквизиты появляются только для компании), контактное лицо, два
  телефона, e-mail, город, адрес объекта, дата замера, откуда пришёл.
- **Двери** — список позиций, свёрнутый до строки «Позиция 1 · Premium Лион · 2050×950 мм».
  Внутри позиции три блока: «Изделие» (линейка Comfort/Premium/Lux и модель — Агора, Ажур,
  Лион, Монтана), «Размеры проёма» и свёрнутые «Характеристики» с материалами. Внизу —
  живой расчёт позиции. Номер позиции проставляется сам и перенумеровывается после удаления.
- **Договор** — номер, дата и загрузка документов: подписанный договор, счёт, акт
  (PDF, фото или Word, до 10 МБ каждый).
- **История сделки** под карточкой — кто, из какого отдела и что сделал: создание, правки
  полей со значениями «было → стало», смена этапов, передача в цех, закрытие этапов цеха
  с исполнителем, списание и возврат материалов, оплаты и документы. Фильтруется по отделу
  (Продажи · Замеры · Завод · Склад · Система) и по типу события. Отдел записывается
  в момент действия: если сотрудник сменит должность, история не перепишется.
- **Оплата** — сверху сумма сделки, оплачено и остаток, рядом кнопка **«Принять оплату»**:
  в окне уже подставлен остаток (при первом платеже — вся сумма договора), поправить можно,
  если клиент платит частями; платёж сохраняется сразу, без «Сохранить» внизу карточки.
  Ниже — список платежей клиента. У каждого платежа сумма, способ, дата, комментарий
  и **обязательный чек** (фото или PDF до 10 МБ): «клиент сказал, что перевёл» потом сверить
  нечем. Предоплата сделки считается сама как сумма платежей, сумма платежей не может
  превысить сумму сделки. Каждый платёж, его правка и удаление пишутся в историю с автором.
- **Сводка справа** видна с любой вкладки: двери по спецификации, доставка, монтаж,
  сумма сделки, предоплата, остаток к оплате. Себестоимости и маржи в карточке нет —
  они нужны в отчётах, а не при работе с клиентом.
- **Управление справа** — ответственный, статус, срок, этап.

Доставка и монтаж задаются в карточке и прибавляются к сумме сделки поверх спецификации
дверей; себестоимость при этом считается только по производству.

### Интерфейс
- **Список сделок** (`/admin/deals`) — сверху полоса показателей: «В работе» с суммой, «Ждут
  завод», «Просрочено» (краснеет, если есть что догонять) и «Ожидаем оплату» — остаток по
  открытым сделкам. Под ней вкладки «Отдел продаж / Завод / Все» и сами сделки — не таблица
  из десяти колонок, а строки из блоков: название с номером, клиентом и городом в одну строку;
  телефон со звонком по клику; этап и под ним что происходит сейчас («ждёт завод: Покраска»,
  «на этапе 2 дн.», итоговый статус у закрытых); сумма и остаток; срок («просрочено на 3 дн.»,
  «через 5 дн.») и ответственный. У просроченной открытой сделки красная кромка слева. На
  телефоне блоки встают друг под другом, без горизонтальной прокрутки. Поиск по названию,
  номеру, клиенту, компании, телефону и БИН; фильтры по воронке, статусу, этапу, менеджеру
  и источнику. Маржи в списке нет — себестоимость при работе с клиентом не показывается.
  Цех видит только наряды и без денег.
- **Канбан** (`/admin/kanban/sales`, `/admin/kanban/factory`) — перетаскивание карточек
  нативным HTML5 drag & drop на Alpine, без внешних библиотек. На тач-экранах такие события
  не приходят вовсе, поэтому у каждой карточки есть кнопки «← →» на соседние этапы: на
  телефоне это единственный рабочий способ двигать сделку, а на десктопе просто быстрее
  мышки. На последнем этапе цеха («ОТК и Упаковка») стрелке некуда вести, поэтому вместо
  «→» у наряда зелёная кнопка «Готово ✓»: закрывает этап, начисляет сдельную оплату и
  переводит сделку продаж в «Готово к отгрузке» — та же логика, что у планшета цеха. Та же
  кнопка есть под полосой этапов в карточке наряда. Колонка грузит 20 карточек и догружает
  остальные по кнопке — иначе на сотнях заказов страница переставала бы открываться;
  счётчик в шапке показывает полное количество.
- **Планшет цеха** (`/shop`) — публичный экран без логина: вход по коду, выбор исполнителя,
  крупные карточки, кнопки «Взял» и «Готово ✓», автообновление раз в 15 секунд.
  Ни сумм, ни данных клиента. Код выдаётся и перевыпускается в **Настройки → Экран цеха**.
- **Инфопанель** (`/admin`) — свой экран вместо набора виджетов: переключатель периода
  (сегодня / неделя / месяц / прошлый месяц / квартал / год / всё время / свой отрезок) живёт
  в адресной строке, поэтому ссылкой на отчёт можно поделиться. Плитки: продажи, деньги,
  производство, склад, люди; ниже — столбики «приход и расход по месяцам», полосы воронки
  продаж и загрузки цеха, расходы по статьям, менеджеры, источники заявок, выработка цеха,
  что пора закупать. Числа считает `App\Services\DashboardStats` по записям, видимым
  пользователю, а показывать ли блок — решает реестр прав.
- **Роли и доступы** (`/admin/access`, только директор) — матрица «право × роль»: строки
  сгруппированы по разделам, в ячейке уровень доступа, изменение действует сразу. Колонка
  директора закреплена, отличия от рекомендованных значений подсвечены, есть сброс прав роли.
  В базе хранятся только отличия, поэтому свежая установка работает без настройки. В шапке —
  «Создать роль» (копией с существующей), «Настроить роль» (название, цвет, описание, галочки
  поведения, показывать ли её в карточке сотрудника) и «Удалить роль» (только придуманную и
  только без сотрудников).
- **Карточка сделки только для чтения** (`/admin/deals/{id}`) — для тех, у кого уровень «чтение»:
  бухгалтер сверяет суммы и платежи, не имея права править сделку. Ссылки на карточку по всей
  системе идут через `DealResource::cardUrl()` и сами ведут либо на правку, либо на просмотр.
- **Блокировка отгрузки** — действие финансов в списке сделок: с обязательной причиной сделка
  перестаёт двигаться дальше этапа передачи в цех (`ProductionException::shipmentBlocked`),
  причина видна на канбане и под полосой этапов. Снимается тем же действием.
- **Факт материалов** — действие цеха на наряде: списать сверх плана или вернуть излишек.
  Движение склада привязано к наряду и автору, запись уходит в историю сделки.
- **Финансы — обзор** (`/admin/finance`, группа меню «Финансы», только admin/manager) — сумма
  договоров без отменённых, дебиторка по открытым сделкам, поступления с разбивкой касса/банк,
  расходы (сдельная оплата, оклады, закуп материалов), результат, склад по учётной цене; фильтр
  «месяц / за всё время». Цифры считает `App\Services\FinanceSummary`. Остальные разделы финансов —
  счета, поступления, расходы, касса, задолженности, ведомость, бонусы — описаны ниже.
- **Счета** (`/admin/invoices`) — витрина по сделкам продаж: выставлено, оплачено, остаток, срок и
  дни просрочки; вкладки «Ожидают оплату / Просрочены / Оплачены / Все» с итогами; «Принять оплату»
  (та же проверка «не больше остатка») и «Напомнить» (запись в историю сделки). Бейдж в меню —
  просроченные.
- **Поступления** (`/admin/incomes`) — все платежи по сделкам одним списком: итоги (всего, касса,
  банк), фильтр по месяцу, способу и менеджеру, чек, «Добавить поступление» с выбором открытой сделки.
- **Задолженности** (`/admin/debts`) — кому должны: контрагент, категория, сумма, выплачено, остаток,
  срок; вкладки «Открытые / Просроченные / Погашенные». «Оплатить» (администратор) создаёт
  подтверждённый расход и списание со счёта; долг закрывается сам при нулевом остатке, руками с
  остатком не закрывается, с платежами не удаляется.
- **Расходы** (`/admin/expenses`) — категории (аренда, налоги, зарплата, материалы, транспорт, реклама,
  сервисы, коммунальные, прочее), сумма, дата, способ, счёт, контрагент, чек, привязка к сделке.
  Вкладки «На проверке / Оплачено / Все» с суммами. Менеджер вносит и правит свои неподтверждённые,
  администратор подтверждает (чек обязателен, кроме зарплаты и налогов) или отклоняет с причиной;
  подтверждённый расход не удаляется. В сводку и в кассу идут только подтверждённые.
- **Касса и банк** (`/admin/cash`) — счета (касса, банк) с остатком по журналу: начальный остаток +
  приход − расход. Движения создаёт `App\Services\CashLedger` из платежей по сделкам (наличные в
  кассу, карта/Kaspi/перевод в банк) и подтверждённых расходов; правка или удаление источника
  меняет движение. Руками — перевод между счетами и корректировка (администратор, с причиной).
  Старые платежи разносятся командой `gravit:finance-backfill` (идемпотентна).
- **Зарплата** (`/admin/salary`) — ведомость за месяц по всем активным сотрудникам: оклад
  (пропорционально дням, если принят внутри месяца), сдельно по закрытым этапам, утверждённые
  бонусы, удержания и авансы, к выплате, выплачено. «Сформировать» пересчитывает черновики;
  «Утвердить» фиксирует цифры; «Выплатить» (частями) создаёт расход «Зарплата» и списание со счёта.
  Логика — `App\Services\PayrollService`.
- **Бонусы** (`/admin/bonuses`) — сотрудник, месяц, сумма, за что, при желании сделка. Предлагают
  менеджер и администратор, утверждает администратор; утверждённый бонус попадает в ведомость и
  в карточку сотрудника, не удаляется — только снимается с утверждения.
- **Автобонус менеджеру** — при переводе сделки на завершающий этап ответственному менеджеру
  начисляется процент от суммы сделки (`App\Services\BonusAccrual`): один бонус на сделку, в месяц
  закрытия, со ссылкой на сделку и записью в её истории; менеджер получает уведомление. Ставка —
  **Настройки → Финансы** (`/admin/finance-settings`, только администратор): процент и режим
  «утверждать автоматически / администратором». В карточке сотрудника можно задать личную ставку.
- **Моя зарплата** (`/admin/my-salary`) — для любого сотрудника, включая цех: своя ведомость
  (или предварительный расчёт, пока её нет), выплаты, бонусы со статусами, закрытые этапы.
  Чужие цифры недоступны: все запросы привязаны к текущему пользователю.
- **Зарплата цеха** (`/admin/payroll`, группа «Финансы») — сдельно по закрытым этапам за месяц, факт против
  норматива, сотрудники без выработки.
- **Прайс конфигуратора** (`/admin/door-options`) — bento-сетка: сверху сводка (в продаже,
  групп заполнено, со склада, в заказах), под ней поиск и фильтры «Все / Со склада / В заказах /
  Сняты с продажи», дальше карточка на каждую группу опций с прозрачным градиентом своего
  цвета. В строке позиции: название, код, списание со склада, в скольких дверях выбрана,
  цена с единицей, ★ вариант по умолчанию, переключатель «в продаже», правка и меню «⋯»
  (выше, ниже, удалить). Менеджер видит прайс без кнопок правки.
- **Движения склада** (`/admin/stock-movements`) — журнал прихода, списания и возвратов,
  только чтение: остаток производен от этих строк.
- **Стиль** — Light SaaS / glassmorphism: `resources/css/filament/admin/theme.css`.
  Блоки формы, плитки и карточки канбана залиты мягким вертикальным градиентом —
  одинаково белые секции встык сливались в одно полотно.
- **Система поверхностей** вместо градиента на каждом слое. Три уровня, каждый отличается
  от соседнего одним признаком: карточка страницы — белая с рамкой и мягкой тенью на сером
  фоне; блок внутри карточки — плоский светло-серый без тени; блок внутри блока — снова
  белый. Один радиус на уровень, один отступ между крупными блоками. Раньше секция внутри
  вкладки внутри карточки несла три фона, три тени и радиусы 8/12/16 px вперемешку.
- **Компактная плотность** задана токенами (`--gd-*` в `theme.css`), а не разбросана по
  экранам: между полями 14 px вместо 24, заголовок страницы 24 px вместо 30, поля и кнопки
  34 px, верхняя панель 56 px, строка таблицы 57 px вместо 93. Модалки — 14–18 px отступа
  вместо 24 со всех сторон. Шаг меняется в одном месте. Экран цеха сознательно не уплотнён:
  там работают пальцами, и крупные кнопки нужнее.
- **Основной цвет — палитра `Color::Blue`**, а не `Color::hex`: из светлого hex Filament
  строил палитру, у которой тёмный тон не проходил проверку контраста, и кнопки рисовались
  бледными с тёмной подписью, как отключённые.
- **«Сохранить / Отмена» закреплены внизу экрана** плавающей панелью: у карточки сделки правая
  колонка выше формы, и кнопки оказывались далеко под вкладками.
- **Единые правила оформления** заданы один раз в `AppServiceProvider`, а не в каждом
  ресурсе: ширина модалок, компактные секции, полосатые таблицы. Иначе одинаковые по
  смыслу окна получались разной ширины и плотности, и панель выглядела собранной из кусков.
- **Формы ресурсов идут в одну колонку.** По умолчанию Filament ставит секции рядом,
  и в модалке поля сжимались до нечитаемых обрубков — цена «12 000» показывалась как «1»,
  дата рождения как «05.11.198».
  Панель по умолчанию в светлой теме, переключатель у пользователя остаётся.
- **Публичная страница** `/track/{qr_hash}` — статус, все позиции заказа, таймлайн этапов,
  прогресс цеха и QR-код. Адаптивная, светлая и тёмная тема. Себестоимость, маржа
  и внутренние заметки на неё не попадают.

### Клиенты и закрытые сделки
Закрытые сделки (завершённые и отменённые) не пропадают: в списке сделок у них своя вкладка
**«Закрытые»**, а на странице **«Клиенты»** (`/admin/clients`) все сделки продаж свёрнуты по
телефону клиента: имя или компания и БИН из последней сделки, город, сколько сделок и сколько
в работе, на какую сумму заказал, сколько должен, когда была последняя сделка. Поиск — по имени,
телефону, компании и БИН; менеджер с уровнем «только свои» видит только своих клиентов.

Из строки клиента: **«Сделки»** — список всех его сделок, **«Новая сделка»** — карточка
заполняется реквизитами из последней сделки (тип, имя, телефоны, e-mail, компания, БИН, город,
адрес объекта), менеджер только проверяет и переходит к дверям. Отдельной таблицы клиентов нет
намеренно: клиент вводится в сделке, и одно место ввода — меньше расхождений.

### Инфопанель по ролям
Директор и бухгалтер видят инфопанель целиком: продажи, деньги, цех, склад, люди. У всех
остальных сверху блок **«Моя работа»** только со своими цифрами, а общих сводок нет:

- **Рабочий цеха** — сколько этапов закрыл за период и сколько сдельно заработал, какие
  наряды сейчас у него в работе (те, где нажал «Взял»). Загрузку цеха и выработку коллег
  не видит.
- **Начальник производства** — сводка по всему цеху (наряды, застрявшие, загрузка, выработка).
- **Замерщик** — замеры сегодня с временем, клиентом и адресом, просроченные выезды тем же
  списком, сколько на неделе, у каждого выезда кнопка «Замерял». Сумм сделок нет.
- **Менеджер** — свои сделки (как и раньше, через «только свои»), свои замеры сегодня, свой
  бонус за период и сколько ждёт утверждения, ссылка на «Мою зарплату». Таблицы по всем
  менеджерам нет.
- **Кадры** — блок по людям (ведомости, штат).

Логика — `DashboardStats::isFullView()` (полный вид: полное право на воронку продаж или
доступ к финансам), `seesFactoryOverview()` (сводка по цеху) и методы `my*()`.

### Где кнопка «Новая сделка»
Везде, где работает менеджер: в шапке панели на любой странице (справа от поиска),
в заголовке инфопанели, воронки продаж, списка сделок и страницы «Клиенты». Одна кнопка
(`App\Filament\Actions\NewDealAction`) — видна только тем, кто заводит сделки: директору
и менеджеру. У бухгалтера, кадров и цеха воронка продаж на чтение, им она не показывается.

### Карточка сотрудника и профиль
`/admin/users/{id}` — что человек сделал и сколько за это получит:

- **Показатели** кольцами: сделки, доведённые до отгрузки; закрытые наряды; этапы цеха за месяц.
- **Активность за неделю** — действия в системе и закрытые этапы цеха по дням.
- **Зарплата за месяц** = оклад + сдельная выработка из `production_logs`, с долей каждого.
- **Сделки, работа в цеху и последние действия** — списками со ссылками.
- Справа — фото, контакты, дата приёма со стажем, день рождения, оклад.

Свою карточку открывает любой сотрудник, чужую — только руководство. **Оклад видно
своему владельцу и администратору**: менеджеру список коллег нужен для назначения
ответственных, а не для сверки чужих зарплат.

`/admin/profile` — **свой профиль**: имя, телефон, e-mail, пароль и фото. Роль, оклад
и доступ в профиль не вынесены — их правит администратор в разделе «Сотрудники»,
иначе рабочий выписал бы себе оклад директора прямо из профиля. Аватар подтягивается
в шапку панели и в карточку; без фото показываются инициалы.

### Настройки
`config/gravit.php` — стоимость сборки, наценка, шаг округления цены, предельные габариты,
списывать ли материалы и пускать ли наряд при нехватке остатка. Переопределяется через `.env`.

---

## Дизайн

Light SaaS / glassmorphism, компактная плотность. Три уровня поверхностей: карточка страницы → плоский серый блок → белый блок внутри. Просрочка и блокировка — красным везде. Токены — в `resources/css/filament/admin/theme.css`, общие правила Filament (модалки, календарь, действия) — в `AppServiceProvider`. Подтверждения — модалки Filament, на планшете — своя. Адаптив проверен на 390 / 820 / 1680 px. Тёмная тема есть, глазами проверена не везде.

### Адаптивность
Проверено на телефоне (390 px), планшете (820 px) и десктопе:

- **Канбан и планшет цеха** — колонка почти во всю ширину экрана телефона с прокруткой
  по одной (scroll-snap); на планшете видно две-три колонки. Кнопки перемещения увеличены
  до 40 px, чтобы попадать пальцем.
- **Полоса этапов** в карточке сделки до 1024 px прокручивается горизонтально со снапом:
  переносом в четыре ряда она съедала половину экрана.
- **Карточка сделки** — сетки Filament сворачиваются в одну колонку сами; вкладки и полоса
  этапов прокручиваются, сводка уходит под форму.
- **Таблицы** — номер сделки перенесён в описание (отдельной колонкой он съедал треть
  ширины телефона), второстепенные колонки появляются с `md`, `lg` и `xl`.
- **Страница клиента** `/track/{hash}` — одноколоночная вёрстка, крупный QR, светлая
  и тёмная тема.

---

## Уведомления

```bash
php artisan gravit:daily-check       # проверить и уведомить
php artisan gravit:daily-check --dry # только показать
```

Ежедневно в 08:30 (`routes/console.php`) руководству и мастерам приходит сводка:
материалы ниже минимального остатка и этапы, идущие дольше норматива. Рабочих не беспокоит.

```bash
php artisan gravit:server-check       # диск, память, свежесть бэкапа; уведомить директора
php artisan gravit:server-check --dry # только показать
```

Ежедневно в 08:00 — проверка самой машины: свободный диск ниже 1,5 ГБ, доступная память
ниже 150 МБ или ночной бэкап старше 36 часов — уведомление директору. Пороги — в `.env`
(`GRAVIT_MIN_FREE_DISK_MB`, `GRAVIT_MIN_FREE_MEMORY_MB`).

---

## Безопасность

- **Файлы закрыты.** Чеки, договоры, документы и аватары лежат в `storage/app/private`,
  а не в `public/storage`. Наружу файл отдаётся только по подписанной ссылке на 30 минут
  (`App\Support\Uploads\PrivateFiles`), которую получает тот, кто вошёл и открыл карточку.
  Подделать подпись нельзя, прямой адрес не работает. Диск задан один раз для всех полей
  загрузки в `AppServiceProvider`.
- **Двухфакторный вход** через приложение-аутентификатор (Google Authenticator, Яндекс.Ключ,
  1Password): профиль → «Двухфакторная аутентификация». Коды восстановления — на случай потери
  телефона. Секрет и коды хранятся зашифрованными. Директору и бухгалтеру стоит включить в первый день.
- **Вход**: 5 попыток в минуту, дальше пауза. Код планшета — 10 попыток в минуту с адреса.
  Страница клиента — 30 запросов в минуту с адреса.
- **Заголовки** на каждом ответе (`App\Http\Middleware\SecurityHeaders`): нельзя встроить панель
  в чужой iframe, браузер не угадывает тип файла, адреса карточек не утекают в Referer,
  HSTS на год при HTTPS.
- **HTTPS принудительно** при `APP_URL=https://…`, кука сессии только по HTTPS (`SESSION_SECURE_COOKIE`),
  доверенные прокси — `TRUSTED_PROXIES`.
- **Права на сервере**: политики, `Deal::visibleTo()`, `Gate::before` для директора; сумма, статус
  и этап сделки не принимаются из формы. Загрузки ограничены типами (фото, PDF, Word) и 10 МБ.
- **Зависимости**: `composer audit` и `npm audit` без известных уязвимостей на 19 сентября 2026.

### Загрузки: сжатие фото и документов
Всё, что загружают в систему — чеки платежей и расходов, договоры и документы сделки,
документы по долгам, аватары — сжимается при сохранении (`App\Support\Uploads\UploadCompressor`).
Подключено одной настройкой ко всем полям загрузки Filament (`AppServiceProvider`), поэтому
новое поле сжимается само.

- **Фото (JPEG, PNG, WebP)**: разворот по EXIF, длинная сторона до 1600 px, JPEG 82 %.
  Фото чека с телефона 4–8 МБ → 200–400 КБ, всё читается. Картинка с прозрачностью — WebP.
  Уменьшение начинается ещё в браузере (FilePond), поэтому с телефона грузится быстро.
- **PDF**: пересборка через Ghostscript (`gs`), если он есть на сервере; результат берётся,
  только если стал заметно меньше. На виртуальном хостинге `gs` обычно нет — PDF остаются как есть.
- **Word и прочее**: без изменений.
- Снимок, который не помещается в память PHP, сохраняется как есть — лучше оригинал, чем
  потерянный чек.

Настройки в `.env`: `GRAVIT_UPLOAD_MAX_SIDE` (1600), `GRAVIT_UPLOAD_QUALITY` (82),
`GRAVIT_UPLOAD_PDF` (true), `GRAVIT_GHOSTSCRIPT` (путь к `gs`).

---

## Где что лежит

| Что | Где |
|---|---|
| Воронки, наряды, материалы, блокировка отгрузки, отмена | `app/Services/DoorProductionService.php` |
| Реестр прав | `app/Services/AccessControl.php`, `app/Enums/Permission.php`, `app/Enums/AccessLevel.php` |
| Справочник ролей | `app/Models/Role.php`, экран — `app/Filament/Pages/AccessMatrix.php`; `app/Enums/UserRole.php` — только семь встроенных кодов |
| Область видимости записей | `Deal::scopeVisibleTo()` |
| Журнал денег (касса, банк, переводы, платёж по долгу) | `app/Services/CashLedger.php` |
| Ведомость зарплаты | `app/Services/PayrollService.php` |
| Автобонус менеджеру | `app/Services/BonusAccrual.php` |
| Финансовая сводка и инфопанель | `app/Services/FinanceSummary.php`, `app/Services/DashboardStats.php`, `app/Support/Period.php` |
| Расчёт цены двери | `app/Services/DoorPriceCalculator.php` |
| Этапы и прайс (правила целостности) | `app/Services/PipelineStageService.php`, `app/Services/PriceListService.php` |
| История, уведомления, журнал заходов | `app/Observers/DealObserver.php` |
| Уведомление о переносе сделки по воронке (директору и ответственному менеджеру) | `app/Listeners/NotifyStageChange.php` (слушает `DealStageChanged`) |
| Кнопки в уведомлениях колокольчика | `DealResource::openAction()` — на карточку сделки, `MySalary::openAction()` — на «Мою зарплату» |
| Кнопка «Замерял» и запись результата | `app/Filament/Pages/Dashboard.php::measureAction()`, право — `DealPolicy::measure()` |
| Правила сервера для платежей, расходов, долгов, бонусов | `app/Observers/*Observer.php` |
| Настройки (ключ-значение с кэшем) | `app/Models/Setting.php` |
| Боевая цепочка цеха | `database/seeders/GravitFactoryStagesSeeder.php` |
| Общие фильтры таблиц | `app/Support/Filament/TableFilters.php` |
| Развёртывание на бою | `deploy/plesk-deploy.sh` (действие при развёртывании в Plesk), `deploy/build-release.sh` (архив с vendor, если нет Composer), `.env.production.example` |
| Установка на бою | `app/Console/Commands/Install.php`, `database/seeders/ProductionSeeder.php` |
| Планы | раздел «Что не сделано и планы» в конце этого файла |
| Сквозные тесты | `tests/Feature/DealLifecycleTest.php`, `RoleMatrixAcceptanceTest.php`, `ProjectConsistencyTest.php` |

Команды: `gravit:install` (первый запуск на бою), `gravit:daily-check`, `gravit:server-check`, `gravit:backup`, `gravit:finance-backfill` (разнести старые платежи по кассе), `gravit:rebuild-stage-visits`. Состояние машины (диск, память) читает `app/Support/ServerHealth.php`; пороги — `config/gravit.php`, секция `server`.

---

## Тесты и проверка кода

```bash
php artisan test
```

504 теста, 2700 проверок. Покрыто:

- обе автоматизации воронок, включая drag & drop через Livewire;
- идемпотентность передачи в цех, списание по всем позициям, агрегация по материалу;
- возврат материалов при отмене и защита от повторного возврата;
- фиксация сдельной оплаты, запрет движения между чужими воронками и по закрытым сделкам;
- вся математика калькулятора и суммирование позиций сделки;
- права: цех не видит сумм и сделок продаж, менеджер не правит воронки, политика
  блокирует перемещение карточки;
- планшет цеха: вход по коду, ограничение перебора, отсутствие денег на экране;
- лимит и счётчики колонок канбана;
- публичная страница, в том числе что она не раскрывает себестоимость;
- карточка сделки: полоса этапов запускает автоматику и уважает права, полные данные
  клиента сохраняются, доставка и монтаж входят в сумму, остаток считается с предоплаты;
- регламент воронки: этапы нельзя перепрыгнуть, без обязательных полей вперёд не пускает,
  назад пускает всегда;
- валидация формы: телефон только казахстанский, БИН ровно 12 цифр, габариты в пределах
  возможностей цеха, предоплата не больше суммы сделки;
- уведомления замерщику: приходят при назначении и переносе даты, не спамят на других правках,
  без ссылки на карточку (она замерщику не открывается);
- запись замера: кнопка видна замерщику у сегодняшних и просроченных выездов, размеры и
  комментарий ложатся в сделку, в историю идёт ровно одна запись «Замер» (а не она же плюс
  «Изменено: Высота замера…»), менеджер уведомлён, закрытая сделка и наряд завода отказывают,
  чужая сделка менеджеру недоступна, список сделок замерщику по-прежнему отдаёт 403;
- веб-корень: `public/.htaccess` закрывает каталоги приложения и `.env`, загрузки от этого не
  страдают — они на закрытом диске;
- города: список не пуст на чистой базе, введённый однажды город попадает в список, «алматы»
  и «АЛМАТЫ» не становятся вторым и третьим Алматы;
- страница входа: открывается гостю, показывает витрину, поля и кнопка остались
  филаментовскими, верный пароль пускает, неверный нет, заблокированный сотрудник не доходит
  до панели;
- лимит входа: пять неверных паролей подряд блокируют вход на минуту, шестая попытка не
  проходит даже с верным паролем (защиту даёт Filament — тест стережёт её от обновлений);
- себестоимость: материалы считаются по каждой позиции и растут с количеством, а сдельная
  оплата цеха прибавляется один раз на заказ — три двери в наряде не утраивают труд;
- справочник ролей: семь базовых на месте и не удаляются, новая роль создаётся копией прав,
  работает на живом сотруднике, галочки поведения включают рассылку замеров и сдельную оплату,
  роль с сотрудниками не удаляется, код роли уникален и не пустой даже у русского названия,
  вторым директором роль не станет — право на матрицу не выдаётся ни копированием, ни руками;
- уведомления о деньгах сотруднику (бонус начислен, бонус утверждён, зарплата выплачена) ведут
  кнопкой «Моя зарплата» за месяц начисления;
- видимость переноса этапа: директору уходит уведомление, автору переноса — нет; менеджер
  узнаёт, когда его сделку двинул директор и когда завод закрыл наряд («Завод закончил заказ:
  … → Готово к отгрузке» с кнопкой «Открыть сделку»); наряды цеха колокольчик не заливают, в
  списке сделок видно «перенёс {имя}», сделка без журнала заходов список не роняет;
- финансы: сводка сходится с платежами, цехом и складом; расходы — роли, чек при подтверждении,
  запрет удаления подтверждённого, массовое подтверждение; касса — платёж создаёт движение и следует
  за правками, расход списывается только после подтверждения, перевод не меняет сумму, корректировка
  только администратору, backfill не задваивает;
- инфопанель: период меняет цифры, произвольный отрезок и перепутанные границы, поступления
  и прибыль за период, менеджер видит только свои сделки без финансового блока, цех — производство
  без сумм, период сохраняется в адресной строке;
- доступ: рекомендованная матрица совпадает с ТЗ, отличия в базе перекрывают её, директора
  нельзя ограничить, неподдерживаемый уровень отклоняется, уволенный сотрудник теряет доступ;
- приёмка по ролям (`RoleMatrixAcceptanceTest`): каждая из семи ролей открывает ровно свои
  разделы, остальные отдают 403;
- «только свои»: список, канбан, просроченные и прямые ссылки показывают менеджеру лишь его
  сделки, итог воронки скрыт, повышение уровня до полного открывает всё;
- «только чтение»: карточка открывается на просмотр, правка — 403, склад без прихода и правок;
- блокировка отгрузки: сделка не идёт дальше передачи в цех, причина обязательна, ставят только
  финансы; факт материалов меняет остаток и попадает в историю, действие видит только цех;
- автобонус: 2 % при закрытии сделки, один раз, личная ставка важнее общей, ноль выключает, ставку
  меняет только администратор, «Моя зарплата» показывает только своё;
- зарплата и бонусы: ведомость складывает оклад, сдельно и утверждённые бонусы, оклад пропорционален
  дате приёма, удержания только в черновике, утверждение замораживает цифры, выплата создаёт расход
  и списание, переплата отклоняется, обзор берёт ведомость и не считает выплаты дважды, бонус
  утверждает только администратор;
- счета, поступления, задолженности: вкладки и итоги, приём оплаты из списка обновляет сделку и кассу,
  переплата отклоняется, напоминание пишется в историю, платёж по долгу = расход + списание,
  переплата и ручное закрытие с остатком отклоняются, платит только администратор;
- аудит противоречий (`ProjectConsistencyTest`): один наряд на сделку после возврата, статус
  следует за этапом, «Отменить сделку», закрытые наряды неприкосновенны, оплата не уходит
  руководству, планшет не закрывает этап без исполнителя, права на действиях таблицы, удаление
  с нарядом в цеху, платежи не больше суммы на сервере, роли замерщика и рабочего, автоматика
  не выключается, ежедневная проверка считает просрочку как экран, рабочий видит канбан без
  кнопок, просроченный наряд не считается дважды, глобальный поиск, сумма сделки не из формы;
- история: создание и правки пишутся с читаемыми названиями полей, события цеха и склада
  попадают в ленту сделки под своим отделом, служебные поля в ленту не идут, отдел
  замораживается на момент события;
- карточка сотрудника: показатели и зарплата считаются, свою карточку открывает каждый,
  чужой оклад закрыт даже менеджеру, список сотрудников закрыт для цеха;
- профиль: аватар меняет только сам сотрудник, роли и оклада в профиле нет;
- время на этапе: часы копятся за все заходы (34,5 + 2 = 36,5), «2-й заход» виден в карточке и на
  канбане, норматив считается по сумме, для старых сделок — только текущий вход без выдумок;
- просрочка: самая большая — сверху, закрытые не считаются, бейдж в меню, вкладка «застряли на
  этапе» по нормативу, красные метки на канбане и в карточке, срок сдачи обязателен, цех видит
  только наряды;
- сквозной сценарий (`DealLifecycleTest`): сделка с двумя дверями от заявки до закрытия — цена
  равна сумме позиций и услуг, замерщик уведомлён, регламент не пускает без договора, чек
  становится предоплатой, склад списан ровно по расходу каждой опции, сделка заблокирована на
  время работы цеха, сдельная оплата начислена тому, кто закрыл этап, история содержит все
  отделы, закрытие только после полной оплаты, страница клиента говорит правду; отмена наряда
  возвращает склад до тысячных и разрешает повторную передачу; двойная передача даёт один наряд;
- список сделок: сводка считает сделки в работе, ждущие завод, просроченные и остаток к оплате
  без отменённых; в строке видно «ждёт завод» и «просрочено на N дн.» только у открытых; поиск по
  телефону; маржи в списке нет; цех не видит денежную сводку;
- прайс: код и группа не меняются при правке, по умолчанию один вариант в группе, позицию из
  заказа не удалить, снятая с продажи позиция не предлагается новым дверям, но старый заказ
  сохраняется с прежней ценой и списывает её материал; страница — карточки групп, поиск,
  создание в группе, правка, звёздочка, менеджер без правок, цех без доступа;
- сделка ждёт завод: пока наряд в цеху, сделку нельзя сдвинуть ни вперёд, ни назад — ни в
  сервисе, ни стрелкой канбана; завод по-прежнему переводит её сам; после отмены наряда сделка
  снова свободна; поле «Этап» в карточке не обходит воронку, новая сделка стартует с первого этапа;
- этапы воронок: новый этап встаёт перед завершающими, порядок перенумеровывается без
  дыр, удаление со сделками требует переноса и пишет историю, этап цеха с работами и этап
  автоматики не удаляются, автоматика уникальна, последний рабочий этап не убирается,
  скрытый этап пропускается воронкой; страница — добавление, перетаскивание, откат
  неудачного переименования, удаление с переносом, настройки, доступ только администратору;
- платежи: предоплата равна сумме платежей, удаление платежа её уменьшает, без чека платёж
  не сохраняется, сумма платежей не больше суммы сделки, чек загружается из карточки;
- экран цеха: исполнитель выбирается из списка строкой из `<select>`, чужого подставить нельзя;
- открытие всех экранов панели и наличие спецификации в карточке сделки.

### Проверка кода
- `php artisan test` — 504 теста, на SQLite в памяти.
- `composer test:mysql` — те же тесты на локальном MySQL, как на бою.
- `vendor/bin/phpstan analyse` — Larastan, уровень 5, конфиг в `phpstan.neon`. Остаются 48
  замечаний по типизации Filament (`$this->record` объявлен как `Model|int|string`) и связей
  Eloquent — это ограничения анализатора, а не ошибки выполнения; базлайном не скрыты нарочно.
- Аннотации `@property` в моделях сгенерированы `php artisan ide-helper:models --write` —
  перегенерируйте после смены схемы или `casts()`.

---

## Развёртывание

Проект рассчитан на 1 ГБ памяти, 2 vCPU и 15 ГБ диска. Замер: страница панели
занимает 65–75 МБ на пике и 4–50 запросов к базе; три процесса PHP-FPM
покрывают отдел продаж, цех и бухгалтерию одновременно.

- База в бою — MySQL от hoster.kz: в коде нет SQL, привязанного к движку, и весь набор тестов
  проходит на MySQL так же, как на SQLite (`composer test:mysql` поднимает базу `gravit_test`
  на локальном MySQL и гонит тесты). Бэкап на MySQL идёт через `mysqldump` в одной транзакции.
- `.env.production.example` — боевой `.env`: кэш и сессии в файлах, очередь
  синхронная, логи 7 дней уровня `warning`, SQLite в WAL.
- Код — в приватном репозитории `github.com/baproger/gravitdoors.kz` (`main`).
- **Бой — виртуальный хостинг hoster.kz с Plesk**, PHP 8.4: Git-развёртывание из панели,
  `bash deploy/plesk-deploy.sh` как действие при развёртывании, планировщик раз в минуту.
  Без SSH и Composer — архив `deploy/build-release.sh` с `vendor`.
- Зависимости закреплены под PHP 8.3+ (`composer.json` → `config.platform.php`): тесты
  проходят на 8.4 и 8.5.
- В `deploy/` лежит только то, что нужно на этом хостинге: `plesk-deploy.sh` и
  `build-release.sh`. Скрипты установки своего VPS (пакеты, swap, PHP-FPM, nginx, cron,
  rsync без GitHub) удалены вместе с конфигами — на Plesk этим занимается панель, а
  держать в репозитории код, который никто не запускает, значит выдавать его за рабочий.
  При переезде на свой сервер их можно достать из истории git (коммит до «чистка репозитория»).
- `php artisan gravit:install` — первый запуск на бою: миграции, справочники без демо-данных
  (этапы, склад, касса, прайс) и директор. Повторный запуск справочники не трогает.
- `php artisan gravit:backup` — снимок базы через `VACUUM INTO` / `mysqldump` и архив файлов;
  14 копий базы и 3 архива файлов (`--keep`, `--keep-files`); архив файлов пропускается,
  если после него на диске осталось бы меньше порога. Запускается планировщиком в 03:00.
- `php artisan gravit:server-check` — диск, память, свежесть бэкапа и учётные записи с угадываемым паролем (только на бою: на разработке `password` у всех по замыслу); в 08:00 предупреждает директора.
- Уведомления панели опрашиваются раз в 2 минуты, а не каждые 30 секунд: десять открытых
  вкладок иначе давали 20 запросов в минуту на трёх PHP-процессах.
- `public/build` хранится в git: Node на сервере не нужен.

Внутри кода под это же: права, настройки и ворота замера запоминаются на
запрос (`AccessControl`, `Setting`, `FactoryStage::measurementGate`), сводки
инфопанели и финансов считают каждую сумму один раз (`RemembersResults`),
график по месяцам — два сгруппированных запроса вместо 24.

---

### Пошагово в Plesk

Сайт живёт на виртуальном хостинге hoster.kz, тариф «Турбо-1»: 15 ГБ диска, 1 ГБ памяти,
2 vCPU, панель **Plesk**, PHP 8.3 и 8.4. Root-доступа, своих конфигов nginx, swap и systemd
здесь нет — всё делается через панель.

Код лежит **выше** веб-корня: наружу отдаётся только `public/`.

```
/var/www/vhosts/<домен>/
├── gravit/            ← репозиторий (artisan, app, vendor, storage …)
│   └── public/        ← веб-корень домена
└── httpdocs/          ← штатный каталог Plesk, не используется
```

Путь развёртывания в Plesk → Git указывает на `gravit`, **без `/public` на конце**. Один раз
ошиблись — и репозиторий развернулся внутрь веб-корня, откуда наружу смотрели `config`,
`database` и `storage` с чеками клиентов. Теперь `plesk-deploy.sh` такую установку замечает
и останавливается до миграций, а `public/.htaccess` закрывает эти пути в любом случае.

**1. PHP.** Сайты и домены → домен → *Настройки хостинга*: версия PHP **8.4**,
режим **FPM-приложение** (обслуживается nginx). Там же ниже — *Настройки PHP*:

| Параметр | Значение | Зачем |
|---|---|---|
| `memory_limit` | 128M | страница панели — 65–75 МБ на пике |
| `max_execution_time` | 60 | зависший запрос не должен держать процесс |
| `upload_max_filesize` / `post_max_size` | 10M / 12M | чеки и договоры до 10 МБ |
| `opcache.enable` | on | обычно уже включён |
| `opcache.memory_consumption` | 64 | если поле есть |

Дополнительные директивы (поле «Дополнительные директивы конфигурации»), если
панель их принимает:

```
opcache.save_comments = 1
opcache.interned_strings_buffer = 8
opcache.max_accelerated_files = 20000
opcache.jit = off
realpath_cache_size = 4096k
realpath_cache_ttl = 600
expose_php = off
```

Почему так: `save_comments` обязателен — Filament и Livewire читают атрибуты из
комментариев и без них падают. JIT на панели даёт единицы процентов, а буфер
забирает память, которой на 1 ГБ нет. `realpath_cache` важнее, чем кажется:
Laravel с Filament открывают тысячи файлов на запрос.

**Сжатие.** Сайты и домены → домен → *Apache и nginx* → включить сжатие (gzip)
для статики. Правила в `public/.htaccess` работают только там, где файлы отдаёт
Apache; если nginx Plesk обслуживает статику сам, сжатие включается его галочкой.
Проверить после развёртывания:

```bash
curl -sI -H 'Accept-Encoding: gzip' https://erp.gravit1.kz/build/assets/theme-*.css | grep -i 'content-encoding\|cache-control'
```

Должны быть `content-encoding: gzip` и `cache-control: public, max-age=31536000, immutable`.
Без сжатия тема панели едет к клиенту как 676 КБ вместо 76 КБ — на телефоне это
единственное, что заметно на первом заходе.

**2. Git.** Сайты и домены → домен → *Git* → «Добавить репозиторий»:
удалённый репозиторий `git@github.com:baproger/gravitdoors.kz.git`, ветка `main`,
каталог развёртывания `gravit` (не `httpdocs`). Plesk покажет **публичный
SSH-ключ** — добавить его в GitHub: репозиторий → Settings → Deploy keys
(только чтение), или с рабочей машины:

```bash
gh repo deploy-key add key.pub -R baproger/gravitdoors.kz -t "hoster.kz plesk"
```

Режим развёртывания — автоматический (webhook) или ручной кнопкой «Развернуть».
В «Действиях при развёртывании» вписать:

```
bash deploy/plesk-deploy.sh
```

Этот скрипт ставит composer-пакеты, генерирует `APP_KEY`, делает бэкап, накатывает
миграции, кэширует конфиг и перезапускает режим обслуживания. Если действия при
развёртывании у подписки недоступны (нет shell), скрипт запускается из планировщика
(шаг 5 ниже) или используется архив релиза (шаг 4).

**3. Веб-корень.** Сайты и домены → домен → *Настройки хостинга* → «Корневой
каталог документов»: `gravit/public`.

**4. Без Git и Composer: архив релиза.** На рабочей машине
`bash deploy/build-release.sh` собирает `deploy/release/gravit-<дата>.zip` с
`vendor` внутри. Plesk → *Файлы* → загрузить архив в каталог `gravit` →
«Извлечь файлы». Дальше запустить `bash deploy/plesk-deploy.sh` по SSH или через
планировщик (шаг 5 ниже): composer он не найдёт и возьмёт `vendor` из архива.

**5. Планировщик.** Сайты и домены → домен → *Запланированные задачи* →
«Добавить задачу», тип «Выполнить команду», **каждую минуту** (`* * * * *`):

```
/opt/plesk/php/8.4/bin/php /var/www/vhosts/<домен>/gravit/artisan schedule:run
```

Внутри Laravel сам решает, что запускать: 03:00 бэкап, 08:00 проверка сервера,
08:30 сводка по складу и цеху. Путь к PHP нужной версии — именно
`/opt/plesk/php/8.4/bin/php`: `php` без пути на хостинге может оказаться старым.

Через такую же одноразовую задачу удобно выполнять любые artisan-команды, если
нет SSH: например установку (шаг 7).

**6. .env.** Plesk → *Файлы* → `gravit/.env` (создаётся из
`.env.production.example` при первом запуске `plesk-deploy.sh`, либо скопировать
руками). Заполнить:

```
APP_URL=https://<домен>
SESSION_SECURE_COOKIE=true
TRUSTED_PROXIES=127.0.0.1,::1
DB_HOST=localhost
DB_DATABASE=<из Plesk → Базы данных>
DB_USERNAME=<там же>
DB_PASSWORD=<там же>
```

База создаётся в Plesk → *Базы данных* → «Добавить базу данных»; кодировку
выбрать `utf8mb4` (сравнение `utf8mb4_unicode_ci`), если панель спрашивает.

**7. Первый запуск.** По SSH (`cd ~/gravit`) или одноразовой задачей планировщика:

```
/opt/plesk/php/8.4/bin/php artisan gravit:install --name="Имя" --email=director@example.kz --password=<пароль>
```

Команда накатывает миграции, заполняет справочники без демо-данных (этапы обеих
воронок, 13 этапов цеха, склад, касса и банк, прайс) и создаёт директора. Повторный
запуск справочники не трогает.

**8. HTTPS.** Сайты и домены → домен → *SSL/TLS-сертификаты* → Let's Encrypt,
включить «Постоянное перенаправление на HTTPS». `APP_URL` с `https://` — иначе
подписанные ссылки на файлы выйдут с `http://` и не откроются.

**9. Второй фактор.** Войти директором → профиль → «Двухфакторная аутентификация» →
привязать приложение-аутентификатор, сохранить коды восстановления. То же —
бухгалтеру. Файлы (чеки, договоры) лежат в `gravit/storage/app/private` и наружу
не смотрят: веб-корень — только `gravit/public`.

### Бюджет памяти

| Что | Сколько | Зачем столько |
|---|---|---|
| Система + SSH + cron | ~120 МБ | остаётся ОС |
| nginx | ~10 МБ | статика и проксирование в PHP |
| PHP-FPM: **3 процесса × ~75 МБ** | ~230 МБ | одна страница панели занимает 65–75 МБ на пике (замерено) |
| opcache | 64 МБ | скомпилированный код, общий для всех процессов |
| MySQL | 0 на VPS | база на стороне hoster.kz; SQLite, если своя, — внутри процесса PHP |
| Запас | ~500 МБ | на всплески и кэш файловой системы |

Три PHP-процесса — это три одновременных запроса. Для отдела продаж, цеха и
бухгалтерии этого достаточно: планшет цеха опрашивает сервер раз в 15 секунд,
остальные работают руками. Четвёртый и пятый запрос встанут в очередь на
доли секунды, а не уронят сервер в swap.

---

## История разработки

1. **Ядро** — две воронки, автоматизация продажи ⇄ завод, конфигуратор, склад, планшет цеха, страница клиента, роли.
2. **Карточка сделки** под amoCRM: клиент, несколько дверей, полоса этапов, регламент обязательных полей, документы, замерщик с уведомлениями.
3. **История сделки** по отделам, автонумерация, адаптив, карточка сотрудника и профиль.
4. **Платежи** с обязательным чеком вместо поля «предоплата».
5. **Дизайн** — поверхности, плотность, bento-прайс, список сделок из блоков.
6. **Конструктор воронок** с правилами целостности.
7. **Проверка корректности** сквозным сценарием и Larastan.
8. **Просрочка** — срок сдачи обязателен, страница «Просроченные», просроченный замер.
9. **Время на этапе** — журнал заходов вместо «времени с последнего входа».
10. **Кнопка «Готово ✓»** на последнем этапе завода на канбане и в карточке; подтверждения — модалки вместо `confirm()`.
11. **Аудит на противоречия** — найдено и исправлено ~35 несостыковок: второй наряд после возврата готовой сделки, статус не следовал за этапом, сдельная оплата руководству, действия без проверки прав, удаление сделки с нарядом в цеху, ссылки на 403, двойной счёт просрочки, платежи больше суммы, сумма сделки руками, возвраты как приход и другие. Добавлены «Отменить сделку», индексы, глобальный поиск.
12. **Финансы** — Обзор с ДДС, Счета, Поступления, Расходы, Касса и банк, Задолженности, Зарплата (ведомость), Бонусы с автобонусом менеджеру, «Моя зарплата», настройки финансов.
13. **Роли и доступы (часть A плана)** — семь ролей, редактируемая матрица прав, «только свои», «только чтение» с отдельной страницей просмотра, блокировка отгрузки, факт материалов, право «Подтверждение», приёмочный тест по ролям.
14. **Восстановление 13 этапов цеха** после случайного `migrate:fresh`; цепочка вынесена в свой сидер.
15. **Интерфейс** — календарь с месяцем и годом, фильтры «за период» на каждой таблице, компактный переключатель столбцов, вкладки списка сделок сходятся со счётчиками.
16. **Инфопанель** — свой экран с периодом и аналитикой по правам вместо двух виджетов.
17. **Сервер 1 ГБ / 2 vCPU / 15 ГБ** — права, настройки и ворота замера запоминаются на запрос (579 → 4 запроса на матрице прав), сводки считают каждую сумму один раз, график по месяцам 24 → 2 запроса, логи 7 дней, бэкап с ротацией, сборка в git, скрипт обновления и инструкция по PHP-FPM/nginx/cron (скрипты своего VPS позже удалены — бой оказался на Plesk).
18. **MySQL** — убраны два SQLite-специфичных места (`strftime`, `VACUUM INTO`), бэкап через `mysqldump`, `composer test:mysql`, боевой `.env` под hoster.kz.
19. **Чтобы сервер не задыхался** — замер всех страниц (≤ 53 запроса, ≤ 100 мс), опрос уведомлений раз в 2 минуты вместо 30 секунд, бэкап файлов 3 копии вместо 14 с защитой от переполнения диска, `gravit:server-check` (диск, память, бэкап) с уведомлением директору, планировщик без наложений, swap и лимиты nginx на публичные адреса, проверка диска перед обновлением, `gravit:install` для первого запуска без демо-данных; бой оказался виртуальным хостингом с Plesk — `deploy/plesk-deploy.sh`, `deploy/build-release.sh`, зависимости закреплены под PHP 8.3+. Попутно: бэкап определял движок по имени соединения, а не по драйверу; откат миграций падал на SQLite (индекс `bonuses.source`) и на MySQL (индексы `deals`, которые перехватили внешние ключи) — теперь `migrate:reset` проходит на обоих.
20. **Оплата и кнопки в карточке по отделам** — владелец не находил, как закрыть оплату, чтобы завершить сделку: платёж жил в репитере вкладки «Оплата» и требовал «Сохранить». Теперь все действия по сделке в `DealActions`: «Принять оплату» с подставленным остатком (первый платёж — вся сумма договора) в шапке карточки, на вкладке «Оплата» и прямо в подсказке полосы этапов; остальные кнопки сгруппированы по отделам (Отдел продаж / Финансы / Завод / Клиент), те же действия в списке сделок и в «Счетах». Бухгалтер и мастер работают со страницы просмотра — кнопки есть и там.
21. **Сжатие загрузок** — диск 15 ГБ, а фото чека с телефона 4–8 МБ. `UploadCompressor` подключён ко всем полям загрузки одной настройкой: фото до 1600 px и JPEG 82 % (с прозрачностью — WebP), разворот по EXIF, PDF через Ghostscript при его наличии, Word без изменений; уменьшение начинается ещё в браузере. `ext-gd` и `ext-exif` объявлены в composer.
22. **Безопасность перед боем** — файлы переведены на закрытый диск с подписанными ссылками (раньше чек открывался по прямому адресу без входа, а поле без ограничения типов позволило бы положить исполняемый файл в веб-корень); заголовки безопасности на каждый ответ (`SecurityHeaders`: X-Frame-Options, nosniff, Referrer-Policy, Permissions-Policy, HSTS по https); HTTPS принудительно, куки сессии только по https, доверенные прокси; двухфакторный вход через приложение-аутентификатор с кодами восстановления (секрет и коды зашифрованы в базе), включается в профиле. `composer audit` и `npm audit` — без известных уязвимостей.
23. **Замер со временем, закрытые сделки и база клиентов** — `measured_at` стал датой со временем (замерщик получает час выезда), утренний список замеров на день замерщикам и менеджеру, просроченные замеры — директору и менеджеру; вкладка «Закрытые» в списке сделок; страница «Клиенты» (сделки, свёрнутые по телефону, с повторным заказом через `/admin/deals/create?client=<телефон>`). Кнопка «Новая сделка» — в шапке панели на каждой странице и в заголовках инфопанели, воронки, списка и клиентов (`NewDealAction`).
24. **Инфопанель по ролям** — директор и бухгалтер видят всё, остальные — блок «Моя работа» со своими цифрами (рабочий — свои этапы и сдельная оплата, замерщик и менеджер — свои замеры, менеджер — свой бонус); сводки по всему цеху только у начальника производства, директора и финансов.
25. **Замер с инфопанели и видимость переноса этапа** — владелец нашёл две дыры. Первая: `measured_at` — это только *план* выезда, факта замера в системе не было вовсе, и размеры проёма замерщик передавал менеджеру голосом; при этом карточка сделки ему не открывается (в матрице прав у замерщика нет ни воронки, ни списка). Теперь у каждого выезда в плитке «Замеры сегодня» — кнопка «Замерял» с модалкой: высота, ширина, комментарий ложатся в сделку (`measurement_*`), в историю одной записью «Замер» и уведомлением менеджеру; просроченные выезды показываются в той же плитке, потому что страница «Просроченные» замерщику отдаёт 403. Вторая: перенос сделки писался только в историю внутри карточки, и директор не видел, что менеджер двинул заказ — событие `DealStageChanged` диспатчилось в пустоту. Появился слушатель `NotifyStageChange`: уведомление уходит директору и ответственному менеджеру, если двигал не он сам — так менеджер узнаёт, что завод закрыл наряд и сделка стала «Готово к отгрузке» (текст «Завод закончил заказ», зелёная галочка), или что директор перевёл её руками (только воронка продаж: 13 этапов цеха залили бы колокольчик; автор переноса себе не пишет). Заодно приведён в порядок весь колокольчик: у каждого уведомления есть кнопка, которая ведёт туда, куда получателю открыт вход. По сделке — «Открыть сделку» на страницу просмотра (`DealResource::openAction()`): ссылка одна и та же для любого получателя, а не по правам того, кто её создал. По деньгам сотруднику (бонус начислен, бонус утверждён, зарплата выплачена) — «Моя зарплата» за нужный месяц (`MySalary::openAction()`): раздел открыт каждой роли. Без ссылки остаются только уведомления о замере замерщику и мастеру — им карточка сделки отдаёт 403, и кнопка вела бы в никуда. В списке сделок под этапом — строка «перенёс Айгуль М., 5 мин» из журнала заходов и фильтр «Этап изменён». Попутно: `Event::listen` в `AppServiceProvider` дублировал автообнаружение Laravel 12 — слушатель срабатывал дважды.
26. **Роли создаются из панели** — владелец искал в настройках кнопку «Создать роль» и не находил: роли были перечнем в коде (`UserRole`), и «Кладовщика» нельзя было завести, не правя исходники. Роли переехали в справочник `roles`; `users.role` и `role_permissions.role` и так были `varchar`, поэтому данные не трогали. Новая роль заводится копией существующей (32 клика по матрице никто бы не сделал), базовые семь переименовываются, но не удаляются, придуманная удаляется, если на ней никто не работает. `match` по перечню заменён двумя галочками в карточке роли — «ездит на замеры» и «работает в цеху», — от них теперь зависят рассылка о выездах, кнопка «Замерял», сдельная оплата и выбор исполнителя на планшете. Попутно закрыта дыра: право «Роли и доступы» больше не выдаётся никакой роли, кроме директора, — раньше матрица позволяла отдать его менеджеру, и тот мог выписать себе всё остальное.
27. **Себестоимость труда разошлась с зарплатой** — сумма расценок этапов цеха умножалась на количество дверей в заказе (`laborCost() × quantity`), а зарплата платилась один раз на наряд: `production_logs` заводится на заказ, а не на изделие. На заказе из трёх дверей себестоимость считала труд трижды при одинарной выплате, и прибыль на таких заказах была занижена. Владелец решил, что расценка означает «за проход наряда через этап», поэтому труд переехал из позиции (`PriceBreakdown`) в заказ (`DealPriceSummary`), а `estimatedCost` позиции стал честным `materialsCost`. Поймано при подготовке нормативов цеха; расценки в тот момент стояли по нулям, поэтому существующие себестоимости правка не задела.
28. **Чистка, скорость и защита перед публичным репозиторием** — владелец попросил убрать из репозитория лишнее и сделать так, чтобы на сервере было быстро и безопасно. Удалены скрипты и конфиги своего VPS (`deploy.sh`, `bootstrap.sh`, `server-setup.sh`, `push.sh`, nginx, PHP-FPM, opcache, cron): бой — Plesk, где всё это делает панель, а неработающий код в репозитории выдаёт себя за рабочий. Найдены и убраны два обращения к чужим серверам на каждой странице: строка `->font('Inter')` гнала браузер за шрифтом на `fonts.bunny.net` (лишние DNS и TLS до первой буквы), хотя Filament возит Inter с собой и публикует в `public/fonts`; кружки с инициалами Filament просил у `ui-avatars.com`, отправляя туда имена сотрудников, — теперь их рисует `InitialsAvatar` инлайновым SVG на 486 байт, цвет выводится из имени, поэтому у человека он всегда один. В `public/.htaccess` добавлены сжатие (тема панели 676 КБ → 76 КБ, самый тяжёлый файл сайта), годовой кэш для файлов сборки с хэшем в имени и запрет на скачивание дампов, копий конфигов и логов по расширению. К заголовкам добавлены `Content-Security-Policy` (без `script-src`: с `unsafe-eval`, который нужен Alpine, он не защищает, но ломает панель), `Cross-Origin-Opener-Policy` и `X-Permitted-Cross-Domain-Policies`. Всё это под тестами — в том числе запрет на внешние ссылки в разметке, который и поймал `ui-avatars.com`.

29. **Аватар не открывался после загрузки** — фото сохранялось, но ссылка на него возвращала 404. Причина нашлась на стыке двух своих же решений: `public/.htaccess` закрывает `/storage/` (туда однажды попал каталог развёрнутого не туда приложения с логами и чеками), а Laravel с того же адреса отдаёт подписанные ссылки на закрытый диск. Apache отвечает раньше, чем запрос доходит до PHP, поэтому весь набор тестов оставался зелёным: тесты ходят в приложение напрямую. Выдача файлов переехала на `/files` (`config/filesystems.php`, ключ `url` у диска `local`) — два разных смысла у одного адреса развели по разным адресам. Появился `PrivateFileRouteTest`: он берёт выражения прямо из `.htaccess` и прогоняет через них подписанную ссылку и адреса **всех** маршрутов панели, чтобы следующий маршрут с именем вроде `/resources/...` не поломался так же тихо. Существующие файлы не пострадали: в базе лежит путь, а адрес собирается заново на каждый показ.

30. **Репозиторий стал публичным** — проверена вся история git, а не только текущие файлы: ни `.env`, ни дампа базы, ни ключей в ней никогда не было, все совпадения оказались заглушками в инструкциях (`DB_PASSWORD=<пароль>`). Код открытым быть может — безопасность системы на его секретности не держится, — но одно место стало опаснее: README прямо называет пароль демо-записей. Поэтому над таблицей доступов появилось предупреждение, а `gravit:server-check` теперь ищет сотрудников с угадываемым паролем и пишет директору. Проверка работает только на бою: на разработке `password` стоит у всех по замыслу сидера, и ругаться на это значит приучить пропускать предупреждение мимо глаз. Заодно подтверждено запросами снаружи, что перебор шестизначного кода планшета цеха упирается в 10 попыток в минуту, а страница клиента по QR — в 30.

31. **Завершённый наряд дублировал свою сделку** — владелец увидел «Закрытые 1, Все 2» и не нашёл вторую запись ни в одной вкладке: наряд стоял завершённым на этапе «Монтаж», в рабочие вкладки не попадал (там только открытые), в «Закрытые» тоже (там только воронка продаж) — и жил единственной строкой во «Все». А строка эта — копия сделки: тот же клиент, та же сумма, тот же срок. Пока цех работает, наряд остаётся отдельной строкой: это отдельная работа. Как только цех закончил, строка убирается — но только у тех, кому видна сама сделка: у мастера цеха воронки продаж нет, и для него наряд единственная запись о заказе (`Deal::scopeWithoutFinishedOrders()`). Счётчик вкладки «Все» считается своим запросом, поэтому правило повторено и там — иначе число снова разошлось бы с таблицей. Попутно найдено второе: наряд копирует сумму сделки, но не предоплату, и показывал «остаток 120 900 ₸» вплотную к сделке, где стояло «оплачено», — две взаимоисключающие строки про одни и те же деньги. Платежи принадлежат сделке, поэтому у наряда этой строки больше нет. И ещё: список закрытых статусов был выписан в трёх скоупах по-разному — теперь один `DealStatus::closedValues()`.

32. **Панель перестала тяжелеть от накопленных заказов** — владелец попросил убедиться, что сервер потянет. Замер показал четыре места, где запрос уходил на каждую строку списка: кнопка «Пересчитать цену» спрашивала у каждой сделки, есть ли позиции (25 запросов на страницу), кнопка «Передать в производство» искала завершённый наряд (ещё 17), склад дважды на строку выяснял, можно ли удалить материал (50), а инфопанель считала часы на этапе по каждому наряду в цеху — 80 запросов и росло дальше. Ответы переехали в сам запрос списка (`withExists`, догрузка `stageVisits`), а методы модели берут готовое значение, если список его посчитал, и спрашивают базу только в одиночной карточке. Итог на 240 сделках: список сделок 62 → 20 запросов, инфопанель 120 → 41, склад 51 → 13, и **ни одна страница больше не зависит от объёма данных**. Закреплено `PagePerformanceTest`: он открывает двенадцать страниц на пустой базе, на 40 сделках и на 160, и падает, если число запросов вырастет — обычные тесты этого не видят, потому что страница открывается и данные верные, просто запросов в разы больше. Попутно `gravit:server-check` научился замечать несобранный кэш настроек и маршрутов: без `artisan optimize` каждый запрос разбирает настройки заново, снаружи это не увидеть, а панель вязкая — теперь директор получит уведомление с подсказкой проверить действие при развёртывании в Plesk.

33. **Фильтры на рабочих страницах** — владелец просил, чтобы на воронке продаж, воронке цеха, просроченных и складе можно было «через фильтр найти то, что искал». На воронках был только поиск и выбор менеджера, поэтому появилась панель фильтров: город, источник, срок сдачи «с — по», только просроченные и состояние оплаты. Условия собраны в `BoardFilter` — один объект и для счётчика в шапке колонки, и для выборки карточек: разъедься они, колонка показывала бы «12» при семи карточках. Наряд завода копирует у сделки не всё (ни города, ни источника, ни предоплаты у него нет), поэтому такие условия проверяются и у самой записи, и у её сделки — иначе на воронке цеха фильтр по городу всегда давал бы пустой экран, и это выглядело бы поломкой. Кнопка «Фильтры» несёт счётчик выбранного, а над колонками идёт строка «показаны только: …» — полупустая доска должна объяснять себя сама. На «Просроченных» и складе таблицы филаментовские, поэтому фильтры штатные: ответственный, этап, город, воронка, источник, срок и «только с долгом»; на складе добавились единица измерения, «закончились», «не участвуют в прайсе» и дата заведения. Проверка — `PageFiltersTest`: он смотрит, что нужная запись остаётся, лишняя уходит, счётчик сходится с карточками и развёрнутая панель отрисовывается на обеих воронках.

34. **Фильтры в финансах** — продолжение предыдущего пункта по просьбе владельца. Касса, поступления, расходы, долги и бонусы фильтры уже имели; без них оставались три страницы. **Ведомость зарплаты**: сотрудник, должность, статус, «не выплачено полностью» и «с удержаниями или авансом» — первый отвечает на главный вопрос бухгалтера в конце месяца. **Зарплата цеха** — страница своя, не филаментовская таблица, поэтому фильтр сужает сами строки отчёта: сотрудник, этап цеха, «только с выработкой»; под фильтром список «кто ничего не сделал» прячется — он имеет смысл по всему цеху за месяц, а не внутри выборки по одной операции. **Обзор финансов** получил произвольный период вместо одного лишь выбора месяца: можно спросить квартал или «с начала года». Месяц и период взаимно исключают друг друга — два периода сразу неоднозначны. Открытую границу `FinanceSummary::between()` подставляет сама (начало — от первой сделки, конец — сегодня): все суммы внутри считаются через `whereBetween`, и одна пустая дата уронила бы запрос. **Счетам** добавлены город, источник, способ оплаты, «долг от 500 000» и период заведения сделки.

35. **«Финансы — обзор» в bento-сетке** — страница была рядом одинаковых плиток в треть ширины: девять цифр одного размера, и глазу не за что зацепиться. Переведена на ту же систему, что инфопанель: список плиток с размерами, `BentoLayout` раскладывает их на сервере без дыр, рисует `<x-db.tile>`. Своей сетки здесь больше нет намеренно — два похожих, но разных обзора расходились бы в мелочах на каждой правке, а в меню они стоят рядом. Появилась иерархия: результат периода первым и крупно (ради него страницу и открывают), под ним поступления и дебиторка, расходы — плиткой двойной высоты, потому что под цифрой разбивка по статьям. «Касса и банк» и «Мы должны» показываются, только когда есть счета и долги: пустая рамка «Мы должны 0 ₸» — шум, а дыру от спрятанной плитки раскладка закрывает сама. Плитки-ссылки ведут в свой раздел целиком; «Куда идти дальше» ссылкой не стала — внутри неё ссылок много, а вложенные `<a>` браузер разбирает непредсказуемо. На реальных данных сетка легла в четыре ряда по двенадцать колонок без единой пустоты. Попутно убрана ссылка на `finance-plan.md`, которого нет с тех пор, как планы переехали в этот файл.

---

## Что не сделано и планы

**Открытые вопросы владельцу**
- Куда ставить флаг «завершает производство» — на «ОТК и упаковка» (сейчас) или на «Монтаж», если доставку и монтаж делает цех (см. §5).
- Нормативы часов и расценки у семи восстановленных этапов цеха стоят по нулям — проставить в «Этапах воронок».

**Настройки, часть B** (не начата): реквизиты компании и логотип, коэффициенты расчёта из `config/gravit.php` в настройки, справочники вместо перечислений (источники, категории расходов, способы оплаты, причины отказа), тексты и подписи, регламенты уведомлений, журнал изменений настроек.

**Финансы** (шаги 9–12, не начаты): закуп материалов как расход, «Мои расходы» под отчёт, отчёты с экспортом, закрытие периода.

**Известные ограничения**
- Закрытие сделки необратимо из интерфейса.
- Категория и модель двери не влияют на цену — она считается от материалов.
- Переделка готового заказа (второй наряд) не предусмотрена — нужен отдельный «Наряд на переделку».
- Статусы «Отгружено» и «Установлено» есть в перечне, но не ставятся.
- Склад может уходить в минус: на разработке по умолчанию разрешено, на бою выключается строкой `GRAVIT_ALLOW_NEGATIVE_STOCK=false` (она есть в `.env.production.example`).
- События `DealHandedToProduction` и `ProductionCompleted` без слушателей — точки для WhatsApp/1С. У `DealStageChanged` слушатель один — `NotifyStageChange`.
- Колонка `deals.payment_method` не используется; иконка этапа не редактируется в конструкторе; демо-чеки в сидере — `.txt`.
- Не сделаны: печатные формы (наряд с QR, счёт, договор), ТВ-экран с рейтингами, CI.

### Импорт сделок из Excel / CSV — следующая задача

Менеджер загружает таблицу заявок (старая CRM, выгрузка сайта, Google-таблица), сопоставляет
колонки и получает сделки в воронке. Не начато. Четыре вещи, которые в этом проекте устроены
не так, как ждёт «обычная» постановка задачи, — с них надо начинать:

1. **Отдельной таблицы клиентов нет.** Клиент живёт в полях сделки, а «база клиентов» —
   страница `Clients`, которая сворачивает сделки по `client_phone`. Поэтому «привязать к
   существующему клиенту» здесь значит: найти последнюю сделку с тем же телефоном и взять
   из неё реквизиты, если в строке файла их нет, — ровно как уже делает `CreateDeal::fillForm()`
   для повторного заказа. Таблицу `clients` не заводить.
2. **Сумма сделки — производная от дверей.** `syncPricing()` пересчитывает `total_price`
   только если у сделки есть двери. Значит импортированная сумма спокойно ложится в поле и
   живёт там, пока менеджер не добавит спецификацию, — тогда пересчитается по прайсу. Это
   ожидаемо, менять `syncPricing` не нужно, но в подсказке к колонке написать стоит.
3. **Фоновой очереди на хостинге нет.** `QUEUE_CONNECTION=sync`, отдельный процесс держать
   негде. Решение: драйвер `database` (таблицы уже есть) и воркер из планировщика раз в
   минуту с автоостановкой — `queue:work --stop-when-empty --max-time=50 --memory=96`.
   Redis и Horizon не подходят: память 1 ГБ.
4. **Файлы — на закрытом диске.** Загруженная таблица и лог ошибок не должны открываться по
   прямой ссылке: лог отдаётся подписанным маршрутом, который проверяет автора импорта.

Ещё два решения, принятых заранее:

- **`.xls` (Excel 97–2003) не поддерживаем** — его читает только PHPSpreadsheet, а это тяжёлая
  зависимость при лимите 128 МБ на запрос. Принимаем `.csv` и `.xlsx`; `.xlsx` читает
  `openspout/openspout`, уже стоящий как зависимость Filament. Для `.xls` — подсказка
  «сохраните как .xlsx».
- **Регламент этапов при импорте не применяется**: данные исторические, сделка может сразу
  встать на «Договор» без замера. Но этап **«Передано в производство» и дальше запрещены** —
  они запускают автоматику цеха (наряд, списание материалов). Разрешены только этапы с
  `order` меньше, чем у этапа с `triggers_production`.

Шаги: таблицы импорта и очередь → `DealImporter` (колонки, сопоставление, правила) → кнопка
и модалка → отчёт и лог ошибок → документация и приёмка.

